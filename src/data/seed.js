// Seed / reference content for the interview command center.
// User progress (status, confidence, notes, stories) lives in localStorage and
// is merged on top of this. Editing this file and redeploying updates the
// reference material without wiping progress (see store/state.js mergeSeed).

export const seedCandidate = {
  name: 'Raveena Ingale',
  role: 'Software Development Engineer (SDE I)',
  company: 'Amazon',
  office: 'Boston, MA (in-person loop)',
  format: '4 interviews × 60 min · LiveCode',
  recruiter: 'Song Hee (Erika) An — Amazon University Talent Acquisition',
  windowStart: '2026-07-27',
  windowEnd: '2026-08-07',
}

// Availability provided to the recruiter (EDT, 9:00 AM – 5:00 PM).
export const seedAvailability = [
  { id: 'a1', date: '2026-07-29', start: '09:00', end: '17:00', tz: 'EDT' },
  { id: 'a2', date: '2026-07-30', start: '09:00', end: '17:00', tz: 'EDT' },
  { id: 'a3', date: '2026-07-31', start: '09:00', end: '17:00', tz: 'EDT' },
  { id: 'a4', date: '2026-08-03', start: '09:00', end: '17:00', tz: 'EDT' },
  { id: 'a5', date: '2026-08-04', start: '09:00', end: '17:00', tz: 'EDT' },
  { id: 'a6', date: '2026-08-05', start: '09:00', end: '17:00', tz: 'EDT' },
  { id: 'a7', date: '2026-08-06', start: '09:00', end: '17:00', tz: 'EDT' },
  { id: 'a8', date: '2026-08-07', start: '09:00', end: '17:00', tz: 'EDT' },
]

export const seedLogistics = {
  livecodeLink: '',
  lunchBuddy: true,
  dietary: 'None',
  accommodations: 'None required',
  saveTheDateReceived: false,
  travelNotes: 'Boston, MA corporate office. Travel details arrive with the Save-the-Date email.',
  flightInfo: '',
  hotelInfo: '',
  bringChecklist: 'Laptop, charger, Wi-Fi capable, photo ID, copies of resume, questions for interviewers.',
}

// ---------------------------------------------------------------------------
// Coding problems. Backbone = the NeetCode 150 (pattern-based), with an Amazon
// frequency overlay (freq: high | medium | low) and a `neet` flag so the list
// can be filtered to just the 150. A few Amazon-signature problems not in the
// 150 are appended. Object-oriented design lives in its own topic.
// ---------------------------------------------------------------------------
const slugify = (t) => t.toLowerCase().replace(/[^a-z0-9]+/g, '-').replace(/^-+|-+$/g, '')
const URL_OVERRIDE = {
  'Pow(x, n)': 'powx-n',
  'Insert Delete GetRandom O(1)': 'insert-delete-getrandom-o1',
}

// Amazon frequency overlay by canonical title.
const HIGH = new Set([
  'Two Sum', 'Best Time to Buy and Sell Stock', 'Product of Array Except Self', 'Group Anagrams',
  'Longest Consecutive Sequence', '3Sum', 'Container With Most Water', 'Trapping Rain Water',
  'Longest Substring Without Repeating Characters', 'Minimum Window Substring', 'Valid Parentheses',
  'Search in Rotated Sorted Array', 'Koko Eating Bananas', 'Merge Two Sorted Lists', 'Merge k Sorted Lists',
  'Copy List with Random Pointer', 'Add Two Numbers', 'LRU Cache', 'Binary Tree Level Order Traversal',
  'Binary Tree Right Side View', 'Lowest Common Ancestor of a Binary Search Tree', 'Subtree of Another Tree',
  'Serialize and Deserialize Binary Tree', 'Word Search II', 'K Closest Points to Origin',
  'Find Median from Data Stream', 'Number of Islands', 'Rotting Oranges', 'Course Schedule',
  'Course Schedule II', 'Word Ladder', 'Alien Dictionary', 'Longest Palindromic Substring', 'Coin Change',
  'Word Break', 'Merge Intervals', 'Meeting Rooms II', 'Maximum Subarray', 'Jump Game', 'Partition Labels',
  // Amazon-signature extras
  'Reorder Data in Log Files', 'Critical Connections in a Network', 'Search Suggestions System',
  'Top K Frequent Words', 'Fruit Into Baskets', 'Reorganize String', 'Binary Tree Vertical Order Traversal',
  'Capacity To Ship Packages Within D Days', 'Find First and Last Position of Element in Sorted Array',
  'Find Peak Element',
])
const MED = new Set([
  'Valid Anagram', 'Contains Duplicate', 'Top K Frequent Elements', 'Encode and Decode Strings',
  'Two Sum II - Input Array Is Sorted', 'Longest Repeating Character Replacement', 'Min Stack',
  'Daily Temperatures', 'Search a 2D Matrix', 'Find Minimum in Rotated Sorted Array',
  'Median of Two Sorted Arrays', 'Time Based Key-Value Store', 'Reorder List', 'Remove Nth Node From End of List',
  'Find the Duplicate Number', 'Reverse Nodes in k-Group', 'Validate Binary Search Tree',
  'Kth Smallest Element in a BST', 'Construct Binary Tree from Preorder and Inorder Traversal',
  'Binary Tree Maximum Path Sum', 'Diameter of Binary Tree', 'Implement Trie (Prefix Tree)',
  'Design Add and Search Words Data Structure', 'Subsets', 'Combination Sum', 'Permutations', 'Word Search',
  'Letter Combinations of a Phone Number', 'Kth Largest Element in an Array', 'Task Scheduler', 'Design Twitter',
  'Clone Graph', 'Pacific Atlantic Water Flow', 'Surrounded Regions',
  'Number of Connected Components in an Undirected Graph', 'Graph Valid Tree', 'House Robber', 'House Robber II',
  'Decode Ways', 'Maximum Product Subarray', 'Longest Increasing Subsequence', 'Unique Paths',
  'Longest Common Subsequence', 'Edit Distance', 'Insert Interval', 'Non-overlapping Intervals', 'Gas Station',
  'Rotate Image', 'Spiral Matrix', 'Set Matrix Zeroes', 'Single Number', 'Number of 1 Bits', 'Counting Bits',
  'Missing Number', 'Reverse Integer', 'Reverse Linked List', 'Reverse Bits',
  // extras
  'Most Common Word', 'Analyze User Website Visit Pattern', 'All Nodes Distance K in Binary Tree',
  'Concatenated Words', 'Minimum Cost to Connect Sticks', 'Maximum Ice Cream Bars', 'Basic Calculator II',
  'Insert Delete GetRandom O(1)', 'LFU Cache', 'Boats to Save People', 'Binary Tree Zigzag Level Order Traversal',
])
const fq = (t) => (HIGH.has(t) ? 'high' : MED.has(t) ? 'medium' : 'low')

// Problems reported in specific Amazon SDE-1 loop rounds, per candidate
// handbooks/experiences (AccioJob Amazon SDE-1 kit + LeetCode Discuss). Values
// are the round tags shown as a column in the Coding list.
const REPORTED = {
  // Round 1 — DSA + LP
  'Spiral Matrix': ['r1'],
  'Rotate Image': ['r1'],
  'Find All Anagrams in a String': ['r1'],
  'Count Univalue Subtrees': ['r1'],
  'Maximum Width of Binary Tree': ['r1'],
  'All Nodes Distance K in Binary Tree': ['r1'],
  'Lowest Common Ancestor of a Binary Tree': ['r1'],
  'Minimum Path Sum': ['r1'],
  'Clone Graph': ['r1'],
  'Rotting Oranges': ['r1'],
  'Search in Rotated Sorted Array': ['r1'],
  'Generate Parentheses': ['r1'],
  'Maximum Product Subarray': ['r1'],
  'Decode Ways': ['r1'],
  'Minimum Falling Path Sum': ['r1'],
  'Kth Largest Element in a Stream': ['r1'],
  'Find the Celebrity': ['r1'],
  // Round 2 — DSA / Machine coding + LP
  'Group Anagrams': ['r2'],
  'Kth Smallest Element in a Sorted Matrix': ['r2'],
  'Open the Lock': ['r2'],
}

const mk = (title, topic, difficulty, neet) => ({
  id: slugify(title),
  title,
  topic,
  difficulty,
  freq: fq(title),
  neet,
  reported: REPORTED[title] || [],
  url: 'https://leetcode.com/problems/' + (URL_OVERRIDE[title] || slugify(title)) + '/',
  status: 'todo',
  confidence: 0,
  notes: '',
  lastPracticed: '',
})

// The NeetCode 150, grouped by our topic names. [title, difficulty]
const NEET = {
  'Arrays & Hashing': [
    ['Contains Duplicate', 'Easy'], ['Valid Anagram', 'Easy'], ['Two Sum', 'Easy'],
    ['Group Anagrams', 'Medium'], ['Top K Frequent Elements', 'Medium'], ['Encode and Decode Strings', 'Medium'],
    ['Product of Array Except Self', 'Medium'], ['Valid Sudoku', 'Medium'], ['Longest Consecutive Sequence', 'Medium'],
  ],
  'Two Pointers': [
    ['Valid Palindrome', 'Easy'], ['Two Sum II - Input Array Is Sorted', 'Medium'], ['3Sum', 'Medium'],
    ['Container With Most Water', 'Medium'], ['Trapping Rain Water', 'Hard'],
  ],
  'Sliding Window': [
    ['Best Time to Buy and Sell Stock', 'Easy'], ['Longest Substring Without Repeating Characters', 'Medium'],
    ['Longest Repeating Character Replacement', 'Medium'], ['Permutation in String', 'Medium'],
    ['Minimum Window Substring', 'Hard'], ['Sliding Window Maximum', 'Hard'],
  ],
  'Stack': [
    ['Valid Parentheses', 'Easy'], ['Min Stack', 'Medium'], ['Evaluate Reverse Polish Notation', 'Medium'],
    ['Generate Parentheses', 'Medium'], ['Daily Temperatures', 'Medium'], ['Car Fleet', 'Medium'],
    ['Largest Rectangle in Histogram', 'Hard'],
  ],
  'Binary Search': [
    ['Binary Search', 'Easy'], ['Search a 2D Matrix', 'Medium'], ['Koko Eating Bananas', 'Medium'],
    ['Find Minimum in Rotated Sorted Array', 'Medium'], ['Search in Rotated Sorted Array', 'Medium'],
    ['Time Based Key-Value Store', 'Medium'], ['Median of Two Sorted Arrays', 'Hard'],
  ],
  'Linked List': [
    ['Reverse Linked List', 'Easy'], ['Merge Two Sorted Lists', 'Easy'], ['Reorder List', 'Medium'],
    ['Remove Nth Node From End of List', 'Medium'], ['Copy List with Random Pointer', 'Medium'],
    ['Add Two Numbers', 'Medium'], ['Linked List Cycle', 'Easy'], ['Find the Duplicate Number', 'Medium'],
    ['LRU Cache', 'Medium'], ['Merge k Sorted Lists', 'Hard'], ['Reverse Nodes in k-Group', 'Hard'],
  ],
  'Trees': [
    ['Invert Binary Tree', 'Easy'], ['Maximum Depth of Binary Tree', 'Easy'], ['Diameter of Binary Tree', 'Easy'],
    ['Balanced Binary Tree', 'Easy'], ['Same Tree', 'Easy'], ['Subtree of Another Tree', 'Easy'],
    ['Lowest Common Ancestor of a Binary Search Tree', 'Medium'], ['Binary Tree Level Order Traversal', 'Medium'],
    ['Binary Tree Right Side View', 'Medium'], ['Count Good Nodes in Binary Tree', 'Medium'],
    ['Validate Binary Search Tree', 'Medium'], ['Kth Smallest Element in a BST', 'Medium'],
    ['Construct Binary Tree from Preorder and Inorder Traversal', 'Medium'],
    ['Binary Tree Maximum Path Sum', 'Hard'], ['Serialize and Deserialize Binary Tree', 'Hard'],
  ],
  'Tries': [
    ['Implement Trie (Prefix Tree)', 'Medium'], ['Design Add and Search Words Data Structure', 'Medium'],
    ['Word Search II', 'Hard'],
  ],
  'Backtracking': [
    ['Subsets', 'Medium'], ['Combination Sum', 'Medium'], ['Permutations', 'Medium'], ['Subsets II', 'Medium'],
    ['Combination Sum II', 'Medium'], ['Word Search', 'Medium'], ['Palindrome Partitioning', 'Medium'],
    ['Letter Combinations of a Phone Number', 'Medium'], ['N-Queens', 'Hard'],
  ],
  'Heaps': [
    ['Kth Largest Element in a Stream', 'Easy'], ['Last Stone Weight', 'Easy'], ['K Closest Points to Origin', 'Medium'],
    ['Kth Largest Element in an Array', 'Medium'], ['Task Scheduler', 'Medium'], ['Design Twitter', 'Medium'],
    ['Find Median from Data Stream', 'Hard'],
  ],
  'Graphs': [
    ['Number of Islands', 'Medium'], ['Clone Graph', 'Medium'], ['Max Area of Island', 'Medium'],
    ['Pacific Atlantic Water Flow', 'Medium'], ['Surrounded Regions', 'Medium'], ['Rotting Oranges', 'Medium'],
    ['Walls and Gates', 'Medium'], ['Course Schedule', 'Medium'], ['Course Schedule II', 'Medium'],
    ['Redundant Connection', 'Medium'], ['Number of Connected Components in an Undirected Graph', 'Medium'],
    ['Graph Valid Tree', 'Medium'], ['Word Ladder', 'Hard'],
    ['Reconstruct Itinerary', 'Hard'], ['Min Cost to Connect All Points', 'Medium'], ['Network Delay Time', 'Medium'],
    ['Swim in Rising Water', 'Hard'], ['Alien Dictionary', 'Hard'], ['Cheapest Flights Within K Stops', 'Medium'],
  ],
  'Dynamic Programming': [
    ['Climbing Stairs', 'Easy'], ['Min Cost Climbing Stairs', 'Easy'], ['House Robber', 'Medium'],
    ['House Robber II', 'Medium'], ['Longest Palindromic Substring', 'Medium'], ['Palindromic Substrings', 'Medium'],
    ['Decode Ways', 'Medium'], ['Coin Change', 'Medium'], ['Maximum Product Subarray', 'Medium'],
    ['Word Break', 'Medium'], ['Longest Increasing Subsequence', 'Medium'], ['Partition Equal Subset Sum', 'Medium'],
    ['Unique Paths', 'Medium'], ['Longest Common Subsequence', 'Medium'],
    ['Best Time to Buy and Sell Stock with Cooldown', 'Medium'], ['Coin Change II', 'Medium'],
    ['Target Sum', 'Medium'], ['Interleaving String', 'Medium'], ['Longest Increasing Path in a Matrix', 'Hard'],
    ['Distinct Subsequences', 'Hard'], ['Edit Distance', 'Medium'], ['Burst Balloons', 'Hard'],
    ['Regular Expression Matching', 'Hard'],
  ],
  'Greedy': [
    ['Maximum Subarray', 'Medium'], ['Jump Game', 'Medium'], ['Jump Game II', 'Medium'], ['Gas Station', 'Medium'],
    ['Hand of Straights', 'Medium'], ['Merge Triplets to Form Target Triplet', 'Medium'], ['Partition Labels', 'Medium'],
    ['Valid Parenthesis String', 'Medium'],
  ],
  'Intervals': [
    ['Insert Interval', 'Medium'], ['Merge Intervals', 'Medium'], ['Non-overlapping Intervals', 'Medium'],
    ['Meeting Rooms', 'Easy'], ['Meeting Rooms II', 'Medium'], ['Minimum Interval to Include Each Query', 'Hard'],
  ],
  'Math & Geometry': [
    ['Rotate Image', 'Medium'], ['Spiral Matrix', 'Medium'], ['Set Matrix Zeroes', 'Medium'], ['Happy Number', 'Easy'],
    ['Plus One', 'Easy'], ['Pow(x, n)', 'Medium'], ['Multiply Strings', 'Medium'], ['Detect Squares', 'Medium'],
  ],
  'Bit Manipulation': [
    ['Single Number', 'Easy'], ['Number of 1 Bits', 'Easy'], ['Counting Bits', 'Easy'], ['Reverse Bits', 'Easy'],
    ['Missing Number', 'Easy'], ['Sum of Two Integers', 'Medium'], ['Reverse Integer', 'Medium'],
  ],
}

// Amazon-signature problems NOT in the NeetCode 150. [title, topic, difficulty]
const AMAZON_EXTRA = [
  ['Reorder Data in Log Files', 'Strings', 'Easy'],
  ['Most Common Word', 'Strings', 'Easy'],
  ['Analyze User Website Visit Pattern', 'Arrays & Hashing', 'Medium'],
  ['Boats to Save People', 'Two Pointers', 'Medium'],
  ['Fruit Into Baskets', 'Sliding Window', 'Medium'],
  ['Basic Calculator II', 'Stack', 'Medium'],
  ['Binary Tree Zigzag Level Order Traversal', 'Trees', 'Medium'],
  ['Binary Tree Vertical Order Traversal', 'Trees', 'Medium'],
  ['All Nodes Distance K in Binary Tree', 'Trees', 'Medium'],
  ['Path Sum', 'Trees', 'Easy'],
  ['Path Sum II', 'Trees', 'Medium'],
  // Reported in Amazon SDE-1 loop rounds (AccioJob handbook + candidate reports)
  ['Find All Anagrams in a String', 'Sliding Window', 'Medium'],
  ['Count Univalue Subtrees', 'Trees', 'Medium'],
  ['Maximum Width of Binary Tree', 'Trees', 'Medium'],
  ['Lowest Common Ancestor of a Binary Tree', 'Trees', 'Medium'],
  ['Minimum Path Sum', 'Dynamic Programming', 'Medium'],
  ['Minimum Falling Path Sum', 'Dynamic Programming', 'Medium'],
  ['Find the Celebrity', 'Graphs', 'Medium'],
  ['Kth Smallest Element in a Sorted Matrix', 'Heaps', 'Medium'],
  ['Open the Lock', 'Graphs', 'Medium'],
  // Binary Search — Amazon-asked, beyond the NeetCode set
  ['Search Insert Position', 'Binary Search', 'Easy'],
  ['First Bad Version', 'Binary Search', 'Easy'],
  ['Find First and Last Position of Element in Sorted Array', 'Binary Search', 'Medium'],
  ['Find Peak Element', 'Binary Search', 'Medium'],
  ['Capacity To Ship Packages Within D Days', 'Binary Search', 'Medium'],
  ['Successful Pairs of Spells and Potions', 'Binary Search', 'Medium'],
  ['Find K Closest Elements', 'Binary Search', 'Medium'],
  ['Longest Subsequence With Limited Sum', 'Binary Search', 'Medium'],
  ['Split Array Largest Sum', 'Binary Search', 'Hard'],
  ['Search Suggestions System', 'Tries', 'Medium'],
  ['Concatenated Words', 'Tries', 'Hard'],
  ['Critical Connections in a Network', 'Graphs', 'Hard'],
  ['Top K Frequent Words', 'Heaps', 'Medium'],
  ['Reorganize String', 'Heaps', 'Medium'],
  ['Minimum Cost to Connect Sticks', 'Heaps', 'Medium'],
  ['Maximum Ice Cream Bars', 'Greedy', 'Medium'],
  ['Insert Delete GetRandom O(1)', 'Design', 'Medium'],
  ['LFU Cache', 'Design', 'Hard'],
]

const ood = (id, title, difficulty, freq) => ({
  id,
  title,
  topic: 'Object-Oriented Design',
  difficulty,
  freq,
  neet: false,
  url: '',
  status: 'todo',
  confidence: 0,
  notes: '',
  lastPracticed: '',
})

export const seedProblems = [
  ...Object.entries(NEET).flatMap(([topic, list]) => list.map(([t, d]) => mk(t, topic, d, true))),
  ...AMAZON_EXTRA.map(([t, topic, d]) => mk(t, topic, d, false)),
  // Object-oriented / low-level design (talk through classes + APIs; no LeetCode link)
  ood('ood-parking-lot', 'Design a Parking Lot', 'Medium', 'high'),
  ood('ood-vending-machine', 'Design a Vending Machine (state machine)', 'Medium', 'high'),
  ood('ood-elevator', 'Design an Elevator System', 'Medium', 'medium'),
  ood('ood-atm', 'Design an ATM', 'Medium', 'medium'),
  ood('ood-cache', 'Design an LRU / LFU Cache (implement)', 'Medium', 'medium'),
  ood('ood-deck-cards', 'Design a Deck of Cards', 'Medium', 'medium'),
  ood('ood-rate-limiter', 'Design a Rate Limiter (token bucket)', 'Medium', 'medium'),
  ood('ood-amazon-locker', 'Design an Amazon Locker', 'Medium', 'medium'),
  ood('ood-file-system', 'Design an In-Memory File System', 'Medium', 'high'),
  ood('ood-library', 'Design a Library System', 'Medium', 'low'),
  ood('ood-tic-tac-toe', 'Design Tic-Tac-Toe (OOD)', 'Medium', 'low'),
]

// Topic reference list (all DSA topics + design areas).
export const seedCodingTopics = [
  'Arrays & Hashing',
  'Strings',
  'Two Pointers',
  'Sliding Window',
  'Stack',
  'Binary Search',
  'Linked List',
  'Trees',
  'Tries',
  'Backtracking',
  'Heaps',
  'Graphs',
  'Intervals',
  'Dynamic Programming',
  'Greedy',
  'Design',
  'Math & Geometry',
  'Bit Manipulation',
  'Object-Oriented Design',
]

// ---------------------------------------------------------------------------
// Amazon Leadership Principles (16). Each has a plain-English "what they want"
// and commonly reported behavioral questions. confidence/notes are user fields.
// ---------------------------------------------------------------------------
const LP = (id, name, emphasis, want, questions) => ({ id, name, emphasis, want, questions })

// Questions below are Amazon's official recruiter question bank (from the LP
// interview template), trimmed to the individual-contributor questions
// (manager-only prompts omitted for an SDE I).
export const seedLPs = [
  LP('customer-obsession', 'Customer Obsession', 'high',
    'Start from the customer and work backwards; earn and keep customer trust.',
    ['Describe a difficult interaction you had with a customer. How did you deal with it? What was the outcome?',
     'Tell me about a time you went above and beyond for a customer. Why did you do it? How did the customer respond?',
     'Give an example of when you anticipated a customer need with a solution they didn’t know they needed yet. How did you know?',
     'Give an example of a time you asked for customer feedback and used it to drive innovation or improvement.',
     'Tell me about a time you evaluated the customer experience of your product/service. What did you do, and the result?',
     'Tell me about a time a customer came to you for something that wouldn’t actually address their need. How did you approach it?',
     'Tell me about a time you had to push back or say no to a customer request.',
     'Tell me about a time you had to balance the needs of the customer with the needs of the business.']),
  LP('ownership', 'Ownership', 'high',
    'Act on behalf of the whole company, think long-term, never say "that\'s not my job".',
    ['Tell me about a time you took on something significant outside your area of responsibility. Why was it important? Outcome?',
     'Describe a time you didn’t think you’d meet a commitment. How did you identify the risk and communicate it to stakeholders?',
     'Give an example of an initiative you undertook because it could benefit the whole company/customers, but wasn’t any group’s responsibility.',
     'Tell me about a time you made a hard decision to sacrifice short-term gain for long-term value for the business.',
     'Describe a time you had to transition a project you owned to a new owner. How did you ensure a smooth handoff?',
     'Tell me about a time you saw a peer struggling and decided to step in and help.']),
  LP('invent-simplify', 'Invent and Simplify', 'medium',
    'Seek new ideas and simplify; "not invented here" is fine — borrow good ideas.',
    ['Give an example of a complex problem you solved with a simple solution. What made it complex?',
     'Describe the most innovative thing you’ve done and why it was innovative. What problem did it solve?',
     'Tell me about a time you made something simpler for customers. What drove the change? Impact?',
     'Describe a time you influenced and drove new thinking / innovation out of your team.',
     'Tell me about a challenging problem the usual approach wouldn’t address. How did you select an alternative, and what did you consider?',
     'Tell me about a novel idea or decision you made that had a big impact.']),
  LP('right-a-lot', 'Are Right, A Lot', 'medium',
    'Strong judgment; seek diverse perspectives and disconfirm your own beliefs.',
    ['Tell me about a time you didn’t have enough data to make the right decision. What did you do? Did it turn out correct?',
     'Tell me about a strategic decision you made without clear data or benchmarks. What alternatives and tradeoffs? How did you mitigate risk?',
     'Tell me about a difficult decision made with input from many sources (customers, stakeholders, partner teams). How did you arrive at it?',
     'Tell me about a time you made a bad decision. What was the impact? What did you learn and how did you apply it?',
     'Tell me about a time you made an error in judgment. What was the impact and the learning?',
     'Tell me about a time you discovered your idea wasn’t the best course of action. How did you find out? What was the better path?',
     'Describe a time you brought different perspectives together to solve a problem. How did you seek out different points of view?']),
  LP('learn-curious', 'Learn and Be Curious', 'high',
    'Always learning, curious about new possibilities and self-improvement.',
    ['Tell me about a time you realized you needed a deeper level of subject-matter expertise to do your job well. What did you do?',
     'Describe a time you took on work outside your comfort area. How did you identify what to learn and build expertise?',
     'Tell me about a time you didn’t know what to do next on a challenging problem. How do you learn what you don’t know?',
     'Give an example of something about yourself you worked on to improve your work effectiveness. What resources helped? Impact?',
     'Give an example of when you explored a new or unexpected area of an existing space. Why? What were the results?',
     'Describe a time someone challenged you to think differently about a problem. How did you respond?',
     'Tell me about a time you used external trends to improve your company’s products or services.']),
  LP('hire-develop', 'Hire and Develop the Best', 'low',
    'Raise the performance bar; coach and mentor others. (New grad: use TA/tutoring/club/onboarding.)',
    ['Tell me about a time you helped a team member develop their career or skills. How did you help? Result?',
     'Give an example of a time you provided feedback to develop the strengths of someone on your team. Did their performance improve?',
     'Tell me about a time you provided coaching for a team member. What started the coaching? Outcome?']),
  LP('highest-standards', 'Insist on the Highest Standards', 'medium',
    'Continually raise the bar; don\'t let defects travel downstream.',
    ['Describe a time you refused to compromise your standards around quality/customer service. Who was the customer? Result?',
     'Tell me about a time you were unsatisfied with the way things were in your team/org. What did you do to change it? Impact?',
     'Tell me about a time you improved the quality of a product/service that was already getting good customer feedback. Why?',
     'Tell me about a time you had to make a decision between standards and delivery. What tradeoffs? Outcome?',
     'Give an example of a goal you’ve had where you wish you had done better. How could you have improved on it?',
     'Describe the most significant continuous-improvement project you led. What was the catalyst and the outcome?']),
  LP('think-big', 'Think Big', 'medium',
    'Bold direction that inspires results; think differently and look around corners.',
    ['Tell me about a time you were working on a goal and saw an opportunity to do something much bigger or better. Did you take it?',
     'Give an example of how you changed the direction or view of a function/department to embrace a new way of thinking.',
     'Give an example of a time you proposed a novel approach to a problem. Why did it require one? Was it successful?',
     'Tell me about a time you drove adoption for your vision/idea. How did you know it was adopted, and how did you track it?',
     'Give an example of an idea or vision you had which was adopted by broad/global stakeholders.',
     'Tell me about a time you thought differently to improve a process that was already working. What assumptions did you question?']),
  LP('bias-action', 'Bias for Action', 'high',
    'Speed matters; many decisions are reversible and don\'t need extensive study.',
    ['Give an example of a calculated risk you took where speed was critical. How did you mitigate the risk? Outcome?',
     'Tell me about a time you worked against tight deadlines and didn’t have time to consider all options. What approach did you take?',
     'Describe a situation where you made an important decision without consulting your manager. How did it turn out?',
     'Tell me about a time you had to gather information and respond immediately to a situation. What was the outcome?',
     'Give an example of when you had to decide between moving forward or gathering more information. What did you do?',
     'Tell me about a time you saw an issue that would impact your team and took a proactive approach to solve it.']),
  LP('frugality', 'Frugality', 'low',
    'Accomplish more with less; constraints breed resourcefulness.',
    ['Give an example of how you helped save costs or eliminate waste within your role or organization. Situation and impact?',
     'Describe a time you had to complete a project or initiative with limited resources. How did you approach it? Impact?',
     'Tell me about a time you didn’t have enough resources for something important but found a creative way to get it done.',
     'Tell me about a time you had to make tradeoffs between quality and cost. How did you weigh the options? Result?',
     'Tell me about a time you generated a creative solution to a problem without requiring additional resources.']),
  LP('earn-trust', 'Earn Trust', 'medium',
    'Listen attentively, speak candidly, treat others respectfully; be self-critical.',
    ['Tell me about a time you had to communicate a change in direction people would have concerns with. How did you understand and mitigate them?',
     'Give an example of a tough or critical piece of feedback you received. What was it, and what did you do about it?',
     'Describe a time you needed to influence a peer who had a differing opinion about a shared goal. What did you do? Outcome?',
     'Give an example of a time you were not able to meet a commitment. What were the obstacles, the impact, and the learning?',
     'Tell me about a time your team’s goals were out of alignment with another team you relied on. How did you work with them?',
     'Tell me about a time you uncovered a significant problem in your team. How did you communicate and address it?',
     'Tell me about a time a team member was struggling at work and you stepped in to help.']),
  LP('dive-deep', 'Dive Deep', 'high',
    'Operate at all levels, audit frequently, no task is beneath you; details matter.',
    ['Tell me about a time you had to dig into the details to understand a complex problem on your team. Where did you find the valuable info?',
     'Tell me about a situation that required you to dig deep to get to the root cause. How did you know you were focusing on the right things?',
     'Tell me about a problem you had to solve that required in-depth thought and analysis. Outcome?',
     'Walk me through a big problem in your organization you helped solve. What info did you gather? What was missing and how did you fill the gaps?',
     'Tell me about a specific metric you used to identify a need for a change. Did you create it? What was the outcome?',
     'Have you ever created a metric that helped identify a need for change? Why did you create it? Outcome?']),
  LP('backbone', 'Have Backbone; Disagree and Commit', 'medium',
    'Respectfully challenge decisions you disagree with, then fully commit.',
    ['Tell me about a time you strongly disagreed with your manager or peer on something important to the business. How did you handle it?',
     'Describe a time you took an unpopular stance in a meeting with peers and your leader. Why did you feel strongly? Outcome?',
     'Give an example of a time you committed to a group decision even though you disagreed. What led you to commit?',
     'Describe a time you felt really strongly about something but the team decided to go a different direction. How hard did you press it?',
     'Tell me about a time you pushed back against a decision that negatively impacted your team. How did it turn out?',
     'Give an example of when you submitted a great idea to your manager and they didn’t support it. How did you handle it?',
     'Describe a time you had to support a business initiative you didn’t agree with. How did you deliver the message to your team?',
     'Tell me about a time the business gained something because you persisted for a length of time.']),
  LP('deliver-results', 'Deliver Results', 'high',
    'Focus on key inputs, deliver with quality and on time; rise to setbacks.',
    ['Give an example of a time you delivered an important project under a tight deadline. What sacrifices did you make? Final outcome?',
     'Tell me about a time you had significant, unanticipated obstacles to overcome in achieving a key goal. Were you successful?',
     'Tell me about a time you not only met a goal but considerably exceeded expectations. How did you do it?',
     'Tell me about a time you were more than halfway to a goal when you realized it might not be the right goal. What did you do?',
     'Give an example of a mission or goal you didn’t think was achievable. How did you help your team try to achieve it?',
     'Tell me about a time something didn’t get completed on time. What was the impact? How do you focus on the right deliverables amid competing priorities?']),
  LP('earths-best-employer', "Strive to be Earth's Best Employer", 'low',
    'Create a safer, more productive, higher-performing, more inclusive workplace; lead with empathy.',
    ['Tell me about a time you helped build a more inclusive working environment. What problems were you addressing? Reactions?',
     'Tell me about a time you advocated for someone who had less influence or seniority than you. Outcome?',
     'Describe a time you implemented a change aimed at improving the work experience of yourself or coworkers.',
     'Describe a time you promoted diversity, equity, or inclusion in the workplace. What approach did you use?',
     'Tell me about a time you showed compassion toward someone during a stressful or high-pressure situation.',
     'Tell me about a time you supported or empowered someone else on your team to achieve a goal.',
     'Tell me about a time you noticed someone was being excluded or treated unfairly. Were you able to help?']),
  LP('broad-responsibility', 'Success and Scale Bring Broad Responsibility', 'low',
    'Consider the broader, second-order impact of your work on the community and world.',
    ['Tell me about a time you worked on a project that had impact beyond your immediate customer/organization. How did you approach the downstream impact?',
     'Tell me about a time you changed a decision because of unintended consequences for your customers.',
     'Tell me about a time you changed course because of the negative impact you were going to cause a third party.',
     'Tell me about a time you thought carefully about social responsibility as a critical aspect of your decision making.',
     'Tell me about a time you considered everyone who was affected by your work, not just your direct customers.',
     'Describe a time you had to balance growth or perceived progress with environmental or societal impacts.',
     'Tell me about a time you realized a project would have unintended, perhaps undesired, future consequences.',
     'Tell me about a time you left something better than how you found it. How did your actions influence things for the better?']),
]

// The ~15 behavioral prompts most reported for new-grad SDE I — prepare first.
export const mostAskedBehavioral = [
  'Tell me about a time you failed / made a mistake, and what you learned.',
  'Tell me about a time you disagreed with a teammate or manager.',
  'Tell me about a time you delivered under a tight deadline.',
  'Tell me about a time you took ownership of something outside your responsibilities.',
  'Tell me about your most challenging project.',
  'Tell me about a time you received critical feedback and how you responded.',
  'Tell me about a time you went above and beyond for a customer or user.',
  'Tell me about a time you had to learn something new quickly.',
  'Tell me about a time you solved a complex problem with a simple solution.',
  'Tell me about a time you made a decision with incomplete data.',
  'Tell me about a time you took a risk or acted fast.',
  'Tell me about a time you had to juggle competing priorities.',
  'Tell me about a time you dug deep to find a root cause.',
  'Tell me about your most significant accomplishment.',
  'Why Amazon?',
]

// ---------------------------------------------------------------------------
// Revision flashcards. deck groups related cards; front = prompt, back = the
// detail to recall. Seeded from dictated notes; you can add your own too.
// ---------------------------------------------------------------------------
const RC = (id, deck, front, back, diagram) => ({ id, deck, front, back, ...(diagram ? { diagram } : {}) })

export const seedRevisionCards = [
  RC('rc-info', 'Abstract Data Structures',
    'What is “information” in a data structure?',
    'The data that actually matters — it can be explicit or implicit. Each ADT category is defined by what it treats as information.'),
  RC('rc-categories', 'Abstract Data Structures',
    'What are the abstract data-structure categories, and what is the “information” in each?',
    '1) Sequence — order is information. 2) Collections — identity is information. 3) Hierarchical — containment is information.'),

  // Sequence
  RC('rc-seq', 'Abstract Data Structures',
    'Sequence category — what is the information?',
    'Order is information. Members: list, stack, monotonic stack, queue, deque, priority queue.'),
  RC('rc-list', 'Abstract Data Structures',
    'List — what is the idea?',
    'Position is the addressing scheme — you access by index (“the 3rd element”, “the 2nd element”). Index matters for access.'),
  RC('rc-stack', 'Abstract Data Structures',
    'Stack — what is the information?',
    'Most-recent-first (LIFO). It preserves the natural ordering of the data provided; entry and exit are at the same end.'),
  RC('rc-monostack', 'Abstract Data Structures',
    'Monotonic stack — how is it different from a stack?',
    'Still preserves ordering, but enforces a monotonic order — either non-increasing or non-decreasing — as elements go in/out.'),
  RC('rc-queue', 'Abstract Data Structures',
    'Queue — what does it encode?',
    'Fairness / priority for the longest-waiting element (FIFO). One end is for entry, the other for exit.'),
  RC('rc-deque', 'Abstract Data Structures',
    'Deque — what is the idea?',
    'Both ends are open. The information lies on the boundaries of a window (front and back).'),
  RC('rc-pq', 'Abstract Data Structures',
    'Priority queue — what is the information?',
    'The ranking of elements defined by a comparator, not arrival time. The most important element is served first, not the earliest.'),

  // Collections
  RC('rc-collections', 'Abstract Data Structures',
    'Collections category — what is the information?',
    'Identity is information. Members: set, multiset, map.'),
  RC('rc-set', 'Abstract Data Structures',
    'Set — what is the idea?',
    'Membership check (is a member present?). No ordering or count — and because it drops order and count, lookup/search is fast.'),
  RC('rc-multiset', 'Abstract Data Structures',
    'Multiset — what does it add over a set?',
    'Membership + count. Still no ordering.'),
  RC('rc-map', 'Abstract Data Structures',
    'Map — what is the idea?',
    'Membership + payload. The payload can be any information — a count, a difference, anything you attach to the key.'),

  // Hierarchical
  RC('rc-hier', 'Abstract Data Structures',
    'Hierarchical category — what is the information?',
    'Containment is information — whether one thing is inside another. Members: tree, binary tree, BST, heap.'),
  RC('rc-tree', 'Abstract Data Structures',
    'Tree — what does it capture?',
    'Natural levels, ownership, and parent–child relationships.'),
  RC('rc-bintree', 'Abstract Data Structures',
    'Binary tree — what is the idea?',
    'A tree with a branching cap of two. No ordering is preserved.'),
  RC('rc-bst', 'Abstract Data Structures',
    'Binary search tree — what ordering does it maintain?',
    'A binary tree with ordering: all left descendants < root < all right descendants.'),
  RC('rc-heap', 'Abstract Data Structures',
    'Heap — what is the information?',
    'A weak ordering invariant that exposes the extreme element (smallest / largest / most-frequent). The information is the extreme, not a full sort.'),

  // Relational (4th category)
  RC('rc-relational', 'Abstract Data Structures',
    'Relational category — what is the information?',
    'Connections / relationships are information — how things link to each other. Members: graph, trie, DAG.\n\n• Graph — arbitrary relationships (nodes + edges); can have cycles.\n• Trie — prefix relationships over strings (each edge is a character).\n• DAG — directed, acyclic; encodes dependencies/ordering (e.g., prerequisites), enabling topological sort.'),

  // ---- Trees & Traversals deck ----
  RC('rc-bt-node', 'Trees & Traversals',
    'Binary tree — what is a node, and the core rules?',
    'A node holds data + pointers to its children (left, right). The entry point is always the root, which points to its children.\n\nRules: a node cannot have more than one parent; each node has at most two children (left child, right child).'),
  RC('rc-subtree', 'Trees & Traversals',
    'What is a subtree?',
    'Any node together with all of its descendants forms a subtree. A tree is made of smaller subtrees — which is why recursion fits so naturally.'),
  RC('rc-tree-examples', 'Trees & Traversals',
    'Real-life examples of tree structures?',
    '• File systems on your desktop (folders inside folders).\n• A Reddit comment thread (replies nesting under comments).\n• A company org chart (the reporting hierarchy).'),
  RC('rc-root', 'Trees & Traversals',
    'What is the root, and how is it used?',
    'The root is the top node and is always given as the input/entry point to the tree — every traversal starts there.'),
  RC('rc-depth', 'Trees & Traversals',
    'How is a node’s depth defined?',
    'Depth = how far a node is from the root.\n\nroot depth = 0\nchild depth = parent’s depth + 1'),
  RC('rc-treenode-code', 'Trees & Traversals',
    'How do you define a tree node in code?',
    'A value plus two child pointers, defaulting to null. If a node has neither left nor right child, it’s a leaf node.',
    'class TreeNode {\n    int value;\n    TreeNode left;    // left child pointer\n    TreeNode right;   // right child pointer\n    TreeNode(int value) { this.value = value; }\n}'),
  RC('rc-leaf', 'Trees & Traversals',
    'What is a leaf node?',
    'A node with no children — both its left and right pointers are null. Leaves are where DFS “bottoms out” and starts backtracking.'),
  RC('rc-traversal-types', 'Trees & Traversals',
    'What are the two families of tree traversal?',
    'DFS (depth-first) and BFS (breadth-first).\n\n• DFS: go in one direction until you hit a leaf, then backtrack. Implemented with recursion or an explicit stack. Sub-types: preorder, inorder, postorder.\n• BFS: visit level by level, using a queue.'),
  RC('rc-dfs-impl', 'Trees & Traversals',
    'How is DFS implemented, and how does it “go back up”?',
    'DFS can be done with recursion or an explicit stack. Going downward is easy — you follow the left/right pointers. But the tree has no parent pointer, so going back up relies on the call stack: it remembers where each call came from and resumes there when a call returns.'),
  RC('rc-callstack', 'Trees & Traversals',
    'How does the recursion call stack enable backtracking in DFS? (interview answer)',
    'A binary tree only has downward pointers (left/right) — there is no parent pointer. So to move back up, DFS leans on the call stack.\n\nEach recursive call is pushed as a frame that stores where it was — the current node and the point to resume at. When a call hits a base case (null or a leaf), it returns and its frame is popped; execution continues in the parent call right after the line that made the child call. That automatic pop-and-resume IS the backtracking — you never store parent pointers yourself.',
    'dfs(1)\n └─ dfs(2)\n     └─ dfs(4)\n         └─ dfs(null) → return, pop\n        ↩ back in dfs(4): go right\n         └─ dfs(null) → return, pop\n        ↩ back in dfs(2): go right → dfs(5) ...\n\nCall stack (top = deepest):\n  ┌───────────┐\n  │  dfs(4)   │  ← returns first\n  │  dfs(2)   │\n  │  dfs(1)   │  ← each frame kept its resume point\n  └───────────┘'),
  RC('rc-bfs', 'Trees & Traversals',
    'How does BFS (level-order) traversal work?',
    'Use a queue. Push the root; then repeatedly pop the whole current level (snapshot the queue size first) and push each node’s children. This visits the tree level by level, top to bottom.'),

  // Traversal orders — same tree used across all three
  RC('rc-preorder', 'Trees & Traversals',
    'Pre-order DFS — what order, and output for the sample tree?',
    'Pre-order = current node BEFORE its children: node → left → right.\n\nFor the tree below: 1, 2, 4, 5, 3.  (Root printed first.)',
    '        1\n       / \\\n      2   3\n     / \\\n    4   5\n\nvoid preorder(TreeNode node) {\n    if (node == null) return;\n    visit(node);          // do work here\n    preorder(node.left);\n    preorder(node.right);\n}'),
  RC('rc-inorder', 'Trees & Traversals',
    'In-order DFS — what order, and output for the sample tree?',
    'In-order = left → current node → right (node in the MIDDLE).\n\nFor the tree below: 4, 2, 5, 1, 3.  (For a BST, in-order yields sorted order.)',
    '        1\n       / \\\n      2   3\n     / \\\n    4   5\n\nvoid inorder(TreeNode node) {\n    if (node == null) return;\n    inorder(node.left);\n    visit(node);          // do work here\n    inorder(node.right);\n}'),
  RC('rc-postorder', 'Trees & Traversals',
    'Post-order DFS — what order, and output for the sample tree?',
    'Post-order = left → right → current node (node LAST).\n\nFor the tree below: 4, 5, 2, 3, 1.  (Great when a node needs its children’s results first — e.g. height, deleting a tree.)',
    '        1\n       / \\\n      2   3\n     / \\\n    4   5\n\nvoid postorder(TreeNode node) {\n    if (node == null) return;\n    postorder(node.left);\n    postorder(node.right);\n    visit(node);          // do work here\n}'),
  RC('rc-traversal-mnemonic', 'Trees & Traversals',
    'Easy way to remember pre / in / post order?',
    'It’s about where the CURRENT node sits relative to its children:\n\n• Node BEFORE children → preorder (root first)\n• Node BETWEEN children → inorder (root in the middle)\n• Node AFTER children → postorder (root last)'),
  RC('rc-balanced', 'Trees & Traversals',
    'Balanced binary tree — what is the condition?',
    'For EVERY node:  |height(left subtree) − height(right subtree)| ≤ 1.\n\nGotcha: it must hold at every node, not just the root. Solve bottom-up — have the recursion return the height, and short-circuit to −1 the moment any subtree is unbalanced, so it stays O(n) instead of O(n²).',
    'int height(TreeNode node) {\n    if (node == null) return 0;\n    int l = height(node.left);\n    if (l == -1) return -1;              // left already unbalanced\n    int r = height(node.right);\n    if (r == -1) return -1;              // right already unbalanced\n    if (Math.abs(l - r) > 1) return -1; // this node unbalanced\n    return 1 + Math.max(l, r);\n}\n// balanced == (height(root) != -1)'),

  // ---- Binary Search Trees (BST) deck ----
  RC('rc-bst-def', 'Binary Search Trees',
    'BST — what is the defining property?',
    'For EVERY node: all values in the left subtree < node’s value < all values in the right subtree. This implies values are unique. And because it holds “at any node”, every subtree of a BST is itself a BST.'),
  RC('rc-bst-ops', 'Binary Search Trees',
    'BST — time complexity of search / insert / delete?',
    'O(log n) on average — each comparison prunes an entire subtree, halving the search space (binary search). Worst case O(n): a degenerate tree that’s basically a straight line / linked list (e.g., inserting sorted values so there are no left children).'),
  RC('rc-bst-search', 'Binary Search Trees',
    'How do you search for a value in a BST?',
    'Start at the root. If target < node.val go left; if target > node.val go right; equal → found. Each step throws away one whole subtree. e.g. find 20 under root 23: 20<23 → left, 20>8 → right, 20>17 → right, found.',
    'boolean search(TreeNode node, int target) {\n    while (node != null) {\n        if (target == node.val) return true;\n        node = target < node.val ? node.left : node.right;\n    }\n    return false;\n}'),
  RC('rc-bst-inorder', 'Binary Search Trees',
    'BST — what does an inorder traversal give you? (the key trick)',
    'An inorder DFS (left → node → right) visits the nodes in SORTED order. This is the single most useful BST fact — it turns “sorted-order” problems into O(n) with no sort.'),
  RC('rc-bst-rangesum', 'Binary Search Trees',
    'Range Sum of BST (938) — how do you use the BST property?',
    'Sum values in [low, high]. Naive DFS visits all nodes O(n). Prune instead: if node.val < low, the whole LEFT subtree is too small → skip it; if node.val > high, the whole RIGHT subtree is too big → skip it. Add node.val only when low ≤ val ≤ high. O(n) worst case, but far fewer visits on average.',
    'int rangeSum(TreeNode node, int low, int high) {\n    if (node == null) return 0;\n    if (node.val < low)  return rangeSum(node.right, low, high);\n    if (node.val > high) return rangeSum(node.left,  low, high);\n    return node.val\n         + rangeSum(node.left,  low, high)\n         + rangeSum(node.right, low, high);\n}'),
  RC('rc-bst-minabsdiff', 'Binary Search Trees',
    'Minimum Absolute Difference in BST (530) — best approach?',
    'Min |a − b| over any two nodes. Since inorder gives sorted order, the answer is always between ADJACENT values in that order. Do an inorder DFS tracking the previous value; take the min gap (node.val − prev). O(n), no sort — beats “collect all + sort” (O(n log n)).',
    'Integer prev = null;\nint best = Integer.MAX_VALUE;\nvoid inorder(TreeNode node) {\n    if (node == null) return;\n    inorder(node.left);\n    if (prev != null) best = Math.min(best, node.val - prev);\n    prev = node.val;\n    inorder(node.right);\n}'),
  RC('rc-bst-validate', 'Binary Search Trees',
    'Validate BST (98) — how do you check it correctly?',
    'Pass an allowed range (small, large) down the tree; require small < node.val < large. Going left: large = node.val. Going right: small = node.val. Root starts with (−∞, +∞). Base case: null → true. Both subtrees must also be valid (AND). Gotcha: comparing a node only to its direct children is WRONG — you must carry the bounds down from ancestors.',
    'boolean dfs(TreeNode node, long small, long large) {\n    if (node == null) return true;\n    if (!(small < node.val && node.val < large)) return false;\n    return dfs(node.left,  small, node.val)\n        && dfs(node.right, node.val, large);\n}\n// call: dfs(root, Long.MIN_VALUE, Long.MAX_VALUE)'),

  // ---- OOP Principles deck (LLD) ----
  RC('rc-oop-encapsulation', 'OOP Principles',
    'Encapsulation — what is it, in one clean line?',
    'Encapsulation bundles data with the methods that operate on it, and hides the internal state behind a clean public interface — so callers use the object through controlled methods instead of touching its fields directly.\n\nWhy it matters:\n• Protects sensitive/critical data — nothing outside the class can put it in an invalid state.\n• Enforces invariants in one place — every change goes through a method that can validate it (e.g. reject a negative amount).\n• Improves security & maintainability — you can change the internals later without breaking callers, because they only depend on the interface.\n\nThe mechanism: make fields private, expose them through public getters/setters (or richer methods) that guard every access.',
    '// Balance is PRIVATE — no one can set it to a bad value directly.\nclass BankAccount {\n    private double balance;      // hidden internal state\n\n    public double getBalance() { // read via a controlled method\n        return balance;\n    }\n\n    public void deposit(double amount) {\n        if (amount <= 0)         // the guard is the whole point\n            throw new IllegalArgumentException("Amount must be > 0");\n        balance += amount;\n    }\n}\n\n// account.balance = -500;      // ✗ won\'t compile — field is private\n// account.deposit(-500);       // ✗ rejected by the guard\n// account.deposit(500);        // ✓ only valid path in')
,
  RC('rc-oop-abstraction', 'OOP Principles',
    'Abstraction — what is it, and how is it different from encapsulation?',
    'Abstraction hides the implementation and exposes only the essential features — you say WHAT an object does, not HOW it does it. The caller programs against a concept (“a payment method can pay”) without knowing the concrete steps behind it.\n\nHow it’s done in Java:\n• Interface — declares only method signatures (the “what”); every class provides its own body (the “how”).\n• Abstract class — can mix declared-but-unimplemented methods with shared concrete code.\n\nWhy it matters:\n• Loose coupling — callers depend on the interface, not the concrete class, so you can swap CreditCard for UPI without touching the caller.\n• Easier maintenance & extension — add a new payment type by writing a new class; existing code is untouched.\n\nEncapsulation vs abstraction (the classic confusion): encapsulation hides DATA (private fields + guarded access); abstraction hides IMPLEMENTATION (the how, behind an interface). One protects state, the other simplifies the contract.',
    '// The abstraction: callers only know "you can pay".\ninterface PaymentMethod {\n    void pay(double amount);   // signature only — no "how"\n}\n\n// One concrete "how".\nclass CreditCard implements PaymentMethod {\n    public void pay(double amount) {\n        charge(amount);        // its own private implementation\n    }\n    private void charge(double amount) { /* talk to card network */ }\n}\n\nclass UpiWallet implements PaymentMethod {\n    public void pay(double amount) { /* totally different "how" */ }\n}\n\n// Caller is coupled to the interface, not the concrete class:\nvoid checkout(PaymentMethod method, double amount) {\n    method.pay(amount);        // works for ANY payment type\n}')
,
  RC('rc-oop-inherit-compose', 'OOP Principles',
    'Inheritance vs Composition — “is-a” vs “has-a”, and which to prefer?',
    'Both are ways to reuse code, but they model different relationships:\n\n• Inheritance = IS-A. A subclass extends a base class and becomes a kind of it. Car extends Vehicle → a Car IS-A Vehicle. Use it only when the subclass is genuinely a specialized type of the parent.\n\n• Composition = HAS-A. A class holds another class as a member (an object field). Car has an Engine → a Car HAS-A Engine (a car is not an engine). You build behavior by wiring objects together.\n\nWhich to prefer: composition, in most cases (“favor composition over inheritance”). Reasons:\n• Flexibility — you can swap the part at runtime (give the Car a different Engine implementation) without changing the Car.\n• Looser coupling — depend on an Engine interface, not a fixed parent, so future implementations plug in cleanly.\n• Inheritance is rigid — it locks you into the parent’s full interface and one hierarchy; deep inheritance trees get fragile fast.\n\nRule of thumb: reach for inheritance only for a true is-a; use composition (has-a) for everything you want to reuse or swap.',
    '// IS-A  → inheritance\nclass Vehicle {\n    void start() { /* shared behavior */ }\n}\nclass Car extends Vehicle {   // a Car IS-A Vehicle\n    void openTrunk() { }\n}\n\n// HAS-A → composition (preferred for reuse/swapping)\ninterface Engine { void run(); }\nclass PetrolEngine  implements Engine { public void run() { } }\nclass ElectricEngine implements Engine { public void run() { } }\n\nclass Car2 {\n    private Engine engine;              // Car HAS-A Engine\n    Car2(Engine engine) {              // injected — swap freely\n        this.engine = engine;\n    }\n    void drive() { engine.run(); }\n}\n\n// new Car2(new PetrolEngine());   // same Car, different engine\n// new Car2(new ElectricEngine()); // no change to Car2 needed')
,
  RC('rc-oop-polymorphism', 'OOP Principles',
    'Polymorphism — the two types, and how does it differ from abstraction?',
    'Polymorphism = “many forms”: the same name/interface behaves differently depending on context. Two kinds:\n\n1) Compile-time (method OVERLOADING) — same method name, different parameter lists (count or types). The compiler picks which one to call based on the arguments. e.g. add(int,int) vs add(int,int,int).\n\n2) Runtime (method OVERRIDING) — a subclass redefines a method it inherited/implemented. Which body runs is decided at RUNTIME by the object’s actual type, not the reference type. e.g. PaymentMethod method = new CreditCard(); method.pay() runs CreditCard’s version.\n\nAbstraction vs polymorphism (they’re related but not the same):\n• Abstraction hides the HOW and exposes a clean contract (interface) — it defines the single shape callers program against.\n• Polymorphism is what lets many concrete types FILL that shape differently, and the right one gets chosen automatically.\nAbstraction sets up the contract; polymorphism is the many implementations behind it, selected by overload (compile) or override (runtime).',
    '// 1) Compile-time — OVERLOADING (same name, different params)\nclass Calculator {\n    int add(int a, int b)        { return a + b; }\n    int add(int a, int b, int c) { return a + b + c; }  // compiler picks by args\n}\n\n// 2) Runtime — OVERRIDING (subclass redefines the method)\ninterface PaymentMethod { void pay(double amount); }\n\nclass CreditCard implements PaymentMethod {\n    public void pay(double amt) { System.out.println("card:  " + amt); }\n}\nclass UpiWallet implements PaymentMethod {\n    public void pay(double amt) { System.out.println("upi:   " + amt); }\n}\n\nPaymentMethod method = new CreditCard();\nmethod.pay(100);   // runs CreditCard.pay — chosen at RUNTIME by actual type\nmethod = new UpiWallet();\nmethod.pay(100);   // now runs UpiWallet.pay — same call site, different form')
,

  // ---- SOLID Principles deck (LLD) ----
  RC('rc-solid-s', 'SOLID Principles',
    'S — Single Responsibility Principle (SRP)',
    'A class should have ONE responsibility — one reason to change. Don’t pile unrelated jobs into a single class; split them so each does one thing.\n\nWhy: when a class does many things, a change to one job risks breaking the others, and the class becomes hard to test and reuse.\n\nExample: an Invoice class that (1) computes totals, (2) saves to the DB, and (3) emails the customer has THREE reasons to change (pricing rules, DB schema, email format). Split it: Invoice holds the data/calculation, InvoiceRepository handles persistence, EmailService handles sending.',
    '// ✗ BAD — one class, three responsibilities\nclass Invoice {\n    double total() { /* ... */ return 0; }\n    void saveToDb() { /* DB code */ }\n    void emailCustomer() { /* email code */ }\n}\n\n// ✓ GOOD — one reason to change each\nclass Invoice {\n    double total() { /* pure calculation */ return 0; }\n}\nclass InvoiceRepository {\n    void save(Invoice inv) { /* DB code */ }\n}\nclass EmailService {\n    void send(Invoice inv) { /* email code */ }\n}'),
  RC('rc-solid-o', 'SOLID Principles',
    'O — Open/Closed Principle (OCP)',
    'Software should be OPEN for extension but CLOSED for modification. You should add new behavior by adding new code (a new class/implementation), not by editing existing, working, tested code.\n\nHow: program to an interface/abstract type; add a new type to extend behavior. The old code that uses the interface never changes.\n\nExample: a payment processor that switches on payment type with if/else must be edited every time you add a method. Instead, define a PaymentMethod interface; each new payment type is a new class — the processor is untouched.',
    '// ✗ BAD — must EDIT this method for every new type\ndouble discount(String type, double amt) {\n    if (type.equals("regular")) return amt;\n    else if (type.equals("premium")) return amt * 0.9;\n    // adding "student" means modifying this method\n    return amt;\n}\n\n// ✓ GOOD — extend by ADDING a class, never editing\ninterface Discount { double apply(double amt); }\nclass Regular  implements Discount { public double apply(double a){ return a; } }\nclass Premium  implements Discount { public double apply(double a){ return a*0.9; } }\nclass Student  implements Discount { public double apply(double a){ return a*0.8; } } // new, no edits\n\ndouble price(Discount d, double amt) { return d.apply(amt); }'),
  RC('rc-solid-l', 'SOLID Principles',
    'L — Liskov Substitution Principle (LSP)',
    'A subclass must be usable ANYWHERE its superclass is expected, without the caller having to change anything or getting surprising behavior. If code works with the base type, swapping in any subclass must not break it.\n\nClassic violation — Square extends Rectangle: a Rectangle lets you set width and height independently. A Square forces width == height, so setWidth(5) secretly also changes height. Code written against Rectangle (“set width 5, height 4, expect area 20”) breaks when handed a Square (area 25). The subclass changed the parent’s contract → LSP violated.\n\nFix: don’t force the inheritance. Model them as separate types under a common Shape abstraction with an area() method, instead of Square IS-A Rectangle.',
    '// ✗ VIOLATION — Square breaks Rectangle’s contract\nclass Rectangle {\n    protected int w, h;\n    void setWidth(int w)  { this.w = w; }\n    void setHeight(int h) { this.h = h; }\n    int area() { return w * h; }\n}\nclass Square extends Rectangle {\n    void setWidth(int w)  { this.w = w; this.h = w; }  // side effect!\n    void setHeight(int h) { this.w = h; this.h = h; }\n}\n// void test(Rectangle r){ r.setWidth(5); r.setHeight(4); assert r.area()==20; }\n// passes for Rectangle, FAILS for Square (area 16) → not substitutable\n\n// ✓ FIX — no forced is-a; common abstraction instead\ninterface Shape { int area(); }\nclass Rect   implements Shape { int w,h; public int area(){ return w*h; } }\nclass Square2 implements Shape { int side; public int area(){ return side*side; } }'),
  RC('rc-solid-i', 'SOLID Principles',
    'I — Interface Segregation Principle (ISP)',
    'No class should be forced to implement methods it doesn’t use. Prefer many small, focused interfaces over one big “fat” interface.\n\nWhy: a fat interface forces implementers to write empty or throw-only methods for features they don’t support — that’s a design smell and it breaks callers who assume every method works.\n\nExample: a single Worker interface with work() and eat() breaks for a RobotWorker (robots don’t eat) — it’s forced to implement eat() with a throw. Split into Workable and Eatable so each class implements only what applies.',
    '// ✗ BAD — fat interface forces unused methods\ninterface Worker {\n    void work();\n    void eat();\n}\nclass Robot implements Worker {\n    public void work() { /* ok */ }\n    public void eat()  { throw new UnsupportedOperationException(); } // smell\n}\n\n// ✓ GOOD — small, focused interfaces\ninterface Workable { void work(); }\ninterface Eatable  { void eat(); }\n\nclass Human implements Workable, Eatable {\n    public void work() { }\n    public void eat()  { }\n}\nclass Robot2 implements Workable {   // only what it actually does\n    public void work() { }\n}'),
  RC('rc-solid-d', 'SOLID Principles',
    'D — Dependency Inversion Principle (DIP)',
    'High-level modules should NOT depend on low-level modules — both should depend on an abstraction (an interface). Don’t hard-wire a class to a concrete implementation; depend on an interface and have the concrete one injected in.\n\nIn one line: don’t directly create the object you depend on. Depend on an interface and let someone else provide the implementation.\n\nWhy: if a Car directly does `new PetrolEngine()`, it’s glued to that one engine. Depend on an Engine interface instead, and you can pass in any implementation (petrol, electric, a mock for testing) without changing the Car. This is the principle behind dependency injection. The testability payoff is huge — an OrderService that does `new MySQLDatabase()` can’t be unit-tested without a real DB; inject a Database interface and you can pass a fake.\n\nNote: this is exactly the composition example from the pillars deck — Car2(Engine engine). “Favor composition over inheritance” + DIP go hand in hand.',
    '// ✗ BAD — high-level Car glued to a concrete engine\nclass Car {\n    private PetrolEngine engine = new PetrolEngine(); // hard-wired\n    void drive() { engine.run(); }\n}\n\n// ✓ GOOD — both depend on the Engine abstraction\ninterface Engine { void run(); }\nclass PetrolEngine   implements Engine { public void run() { } }\nclass ElectricEngine implements Engine { public void run() { } }\n\nclass Car2 {\n    private final Engine engine;      // depends on abstraction\n    Car2(Engine engine) {             // injected from outside\n        this.engine = engine;\n    }\n    void drive() { engine.run(); }\n}\n// new Car2(new ElectricEngine());  // swap freely, Car2 unchanged\n\n// Same idea, testability angle:\n// ✗ BAD — hard-wires a low-level class, can\'t swap or test\nclass OrderService {\n    private Database db = new MySQLDatabase();\n}\n// ✓ GOOD — depend on an abstraction, inject it\ninterface Database { }\nclass OrderService2 {\n    private final Database db;\n    OrderService2(Database db) { this.db = db; } // any Database works\n}'),

  // ---- UML Relationships deck (LLD) ----
  RC('rc-uml-overview', 'UML Relationships',
    'The two big buckets of class relationships — and what’s under each?',
    'Every relationship between classes is either:\n\n• IS-A → inheritance. One class extends/implements another and becomes a kind of it (Car IS-A Vehicle).\n\n• HAS-A → the object holds/uses another object. HAS-A splits into FOUR types, from weakest to strongest coupling:\n   1. Association — just knows about / uses another class.\n   2. Aggregation — “weak has-a”: holds a part that can live on its own.\n   3. Composition — “strong has-a”: owns a part whose life it controls.\n   4. Dependency — temporary use, usually via a method parameter.\n\nInterview tip: when you draw an LLD diagram, naming the exact relationship (“Order aggregates Products, but composes its OrderItems”) signals real design maturity.'),
  RC('rc-uml-isa', 'UML Relationships',
    'IS-A (Inheritance) — what is it?',
    'One class extends another (or implements an interface) and becomes a specialized kind of it. The subclass inherits the parent’s behavior. Use only for a genuine “is a kind of” relationship.\n\nExample: Car IS-A Vehicle.',
    'class Vehicle { void start() { } }\nclass Car extends Vehicle {   // Car IS-A Vehicle\n    void openTrunk() { }\n}'),
  RC('rc-uml-association', 'UML Relationships',
    'Association — the weakest HAS-A. What is it?',
    'One class simply KNOWS ABOUT / uses another — there’s a relationship, but no ownership and no control over the other’s lifecycle. Both objects exist independently; one just holds a reference to interact with the other.\n\nExample: a Teacher and a Student know each other, but neither owns or created the other. (Aggregation and composition are actually special, stronger kinds of association.)',
    '// Association — Teacher references Student, no ownership\nclass Student { }\nclass Teacher {\n    private Student student;      // just knows about a Student\n    void assign(Student s) { this.student = s; }\n}\n// both are created and destroyed independently'),
  RC('rc-uml-aggregation', 'UML Relationships',
    'Aggregation — “weak has-a”. What defines it?',
    'A whole HAS parts, but the parts can EXIST INDEPENDENTLY of the whole. The whole does NOT create the part — it’s passed in from outside. If the whole is destroyed, the part still lives on.\n\nExample: a Company has Employees. The company doesn’t create the employee’s existence — an Employee exists before/after the company, and can move to another company. Shut the company down and the Employee objects still exist. (Think: shared, borrowed parts.)',
    '// Aggregation — Employee passed in, outlives the Company\nclass Employee { }\nclass Company {\n    private List<Employee> employees;\n    Company(List<Employee> employees) {   // parts come from OUTSIDE\n        this.employees = employees;\n    }\n}\n// Employee objects exist with or without the Company'),
  RC('rc-uml-composition', 'UML Relationships',
    'Composition — “strong has-a”. How is it different from aggregation?',
    'A whole OWNS its parts and controls their lifecycle. The part is CREATED INSIDE the whole and cannot meaningfully exist without it — if the whole is destroyed, its parts are destroyed too.\n\nContrast with aggregation: aggregation’s part is passed in and outlives the whole; composition’s part is created inside and dies with the whole.\n\nExample: a House composes its Rooms — the House creates its Rooms, and destroying the House destroys the Rooms (a Room has no life outside its house). Same shape as Car creating its own Engine internally.',
    '// Composition — House CREATES its Rooms, owns their lifecycle\nclass Room { }\nclass House {\n    private final List<Room> rooms = new ArrayList<>();\n    House() {\n        rooms.add(new Room());   // created INSIDE — owned\n        rooms.add(new Room());\n    }\n}\n// destroy the House → its Rooms go with it'),
  RC('rc-uml-dependency', 'UML Relationships',
    'Dependency — the most temporary relationship. What is it?',
    'One class TEMPORARILY uses another — typically the other is passed as a METHOD PARAMETER (or a local variable), not stored as a member field. The relationship exists only for the duration of that method call.\n\nKey difference from association/aggregation/composition: no member variable, no long-term link — “uses-a for a moment.”\n\nExample: an Order’s checkout(PaymentService payment) method uses a PaymentService just to process one payment, then forgets it. Order doesn’t hold a PaymentService field.',
    '// Dependency — used only inside the method, not stored\nclass PaymentService { void pay(double amt) { } }\nclass Order {\n    void checkout(PaymentService payment) {   // passed in, temporary\n        payment.pay(100);                     // used, then gone\n    }\n    // note: no PaymentService field on Order\n}')
,

  // ---- Design Patterns deck (LLD) ----
  RC('rc-dp-singleton', 'Design Patterns',
    'Singleton (creational) — what problem does it solve, and how?',
    'Ensures a class has EXACTLY ONE instance across the whole application, with a single global access point. Use it for things that should be shared and unique — a configuration, a registry, a database connection, a logger.\n\nHow to build it:\n1. A private static field holds the one instance.\n2. Make the constructor private so no one else can call `new`.\n3. Expose a public static getInstance() that creates the instance on first call and returns the same one every time after (lazy initialization).\n\nGotcha: the naive version isn’t thread-safe — two threads can both pass the null check and create two instances. The common fix is the “double-checked locking” with a volatile field (or an eager static instance / enum singleton).',
    'class Config {\n    private static Config instance;          // the one instance\n    private Config() { }                      // private ctor — no external new\n\n    public static Config getInstance() {\n        if (instance == null) {               // create only on first call\n            instance = new Config();\n        }\n        return instance;                      // same object every time\n    }\n}\n\n// Thread-safe (double-checked locking):\nclass Config2 {\n    private static volatile Config2 instance;\n    private Config2() { }\n    public static Config2 getInstance() {\n        if (instance == null) {\n            synchronized (Config2.class) {\n                if (instance == null) instance = new Config2();\n            }\n        }\n        return instance;\n    }\n}'),
  RC('rc-dp-factory', 'Design Patterns',
    'Factory Method (creational) — what is it, and why use it?',
    'A single method decides WHICH concrete class to instantiate and returns it, so the calling code never does `new ConcreteClass()` scattered everywhere. Callers ask the factory for an object (usually by a type/enum) and get back the right subtype through a common base type.\n\nWhy: centralizes object creation in one place. Adding a new type means editing one factory (and adding a class) instead of hunting down every `new` across the codebase — this supports Open/Closed and keeps callers decoupled from concrete classes (Dependency Inversion).\n\nExample (Parking Lot): an abstract ParkingSpot with subclasses MotorcycleSpot, CompactSpot, LargeSpot. A ParkingSpotFactory takes a VehicleType enum and returns the matching spot — the caller just says “give me a spot for a CAR.”',
    'enum VehicleType { MOTORCYCLE, CAR, TRUCK }\n\nabstract class ParkingSpot { }\nclass MotorcycleSpot extends ParkingSpot { }\nclass CompactSpot    extends ParkingSpot { }\nclass LargeSpot      extends ParkingSpot { }\n\nclass ParkingSpotFactory {\n    static ParkingSpot create(VehicleType type) {\n        switch (type) {\n            case MOTORCYCLE: return new MotorcycleSpot();\n            case CAR:        return new CompactSpot();\n            case TRUCK:      return new LargeSpot();\n            default: throw new IllegalArgumentException("Unknown: " + type);\n        }\n    }\n}\n\n// caller never news a concrete spot:\n// ParkingSpot spot = ParkingSpotFactory.create(VehicleType.CAR);')
,
  RC('rc-dp-builder', 'Design Patterns',
    'Builder (creational) — what problem does it solve, and how?',
    'Assembles a complex object STEP BY STEP, instead of via one giant constructor. Use it when a class has many fields — especially several optional ones — where a constructor like new User(name, age, email, phone, address, ...) becomes unreadable and easy to get wrong (which arg was which?).\n\nHow to build it:\n1. Make the target class’s constructor private; it takes a Builder.\n2. Add a static nested Builder class with the same fields.\n3. Each setter on the Builder sets one field and returns `this` (so calls chain fluently).\n4. A build() method constructs and returns the finished object.\n\nResult: readable, named, order-independent construction — and you can make the final object immutable (all fields final, no setters).',
    'class User {\n    private final String name;\n    private final int age;\n    private final String email;\n\n    private User(Builder b) {          // private — only Builder calls it\n        this.name = b.name;\n        this.age = b.age;\n        this.email = b.email;\n    }\n\n    static class Builder {\n        private String name;\n        private int age;\n        private String email;\n\n        Builder name(String n)  { this.name = n;  return this; }  // return this\n        Builder age(int a)      { this.age = a;    return this; }  // → chainable\n        Builder email(String e) { this.email = e;  return this; }\n\n        User build() { return new User(this); }\n    }\n}\n\n// Readable, order-independent, only set what you need:\nUser u = new User.Builder()\n        .name("Raveena")\n        .age(23)\n        .email("r@example.com")\n        .build();')
,
  RC('rc-dp-adapter', 'Design Patterns',
    'Adapter (structural) — what problem does it solve, and how?',
    'Makes two INCOMPATIBLE interfaces work together. Your code expects one interface, but an existing/third-party class exposes a different one — instead of rewriting either, you put an ADAPTER in between that translates one to the other. (Like a physical plug adapter: the socket and the plug don’t change; the adapter bridges them.)\n\nHow to build it:\n1. Your code depends on a target interface (the shape it wants, e.g. PaymentProcessor.pay()).\n2. The incompatible class (the “adaptee”) has a different method (e.g. PayPalSDK.sendPayment()).\n3. The Adapter IMPLEMENTS your target interface and HOLDS the adaptee; its methods translate the call and delegate to the adaptee.\n\nWhy: lets you integrate legacy or third-party code without touching it, and keeps your code coupled only to your own clean interface (supports Open/Closed — new SDK = new adapter, no edits to callers).',
    '// Target — the interface YOUR code wants\ninterface PaymentProcessor {\n    void pay(double amount);\n}\n\n// Adaptee — third-party class with an INCOMPATIBLE method\nclass PayPalSDK {\n    void sendPayment(double amountInDollars, String currency) { /* ... */ }\n}\n\n// Adapter — implements your interface, delegates to the adaptee\nclass PayPalAdapter implements PaymentProcessor {\n    private final PayPalSDK paypal;\n    PayPalAdapter(PayPalSDK paypal) { this.paypal = paypal; }\n\n    public void pay(double amount) {\n        paypal.sendPayment(amount, "USD");   // translate + delegate\n    }\n}\n\n// Caller only knows PaymentProcessor — PayPal is hidden behind the adapter:\n// PaymentProcessor p = new PayPalAdapter(new PayPalSDK());\n// p.pay(100);')
,
  RC('rc-dp-decorator', 'Design Patterns',
    'Decorator (structural) — what problem does it solve, and how?',
    'Adds behavior to an object by WRAPPING it, instead of subclassing. Use it when you have many optional add-ons that can combine freely — subclassing would explode into a class per combination.\n\nThe coffee example: a base coffee costs $5; customers add milk, sugar, chocolate… in any mix. Making a class for every combo (MilkCoffee, MilkSugarCoffee, MilkSugarChocoCoffee…) is a combinatorial explosion. Instead, each add-on is a DECORATOR that wraps a coffee and adds its own cost on top.\n\nHow to build it:\n1. A common interface (Coffee) with the method(s) — e.g. cost().\n2. A concrete base (SimpleCoffee) implements it.\n3. Each decorator ALSO implements Coffee AND holds a Coffee inside it; its cost() = wrapped.cost() + its own extra.\n4. Wrap freely: new Milk(new Sugar(new SimpleCoffee())) — costs stack.\n\nKey idea: you’re composing behavior at runtime by nesting wrappers, not fixing it at compile time with inheritance.',
    'interface Coffee { double cost(); }\n\nclass SimpleCoffee implements Coffee {\n    public double cost() { return 5.0; }        // base\n}\n\n// Base decorator — implements Coffee AND wraps a Coffee\nabstract class CoffeeDecorator implements Coffee {\n    protected final Coffee inner;\n    CoffeeDecorator(Coffee inner) { this.inner = inner; }\n}\n\nclass Milk extends CoffeeDecorator {\n    Milk(Coffee c) { super(c); }\n    public double cost() { return inner.cost() + 0.5; }   // add on top\n}\nclass Sugar extends CoffeeDecorator {\n    Sugar(Coffee c) { super(c); }\n    public double cost() { return inner.cost() + 0.2; }\n}\n\n// Stack add-ons at runtime — no combo classes needed:\n// Coffee c = new Milk(new Sugar(new SimpleCoffee()));\n// c.cost()  →  5.0 + 0.2 + 0.5 = 5.7')
,
  RC('rc-dp-facade', 'Design Patterns',
    'Facade (structural) — what problem does it solve, and how?',
    'Provides ONE simple entry point over a COMPLEX subsystem of many classes. The client calls a single high-level method; the facade coordinates all the messy internal steps behind it.\n\nWhy: without a facade, the caller must know about, create, and orchestrate every subsystem (inventory, payment, shipping…) in the right order — that couples the caller to internals it shouldn’t care about. A facade hides that; the caller just says “place this order.”\n\nExample: an OrderFacade with a single placeOrder() that internally reserves inventory, charges payment, and ships the product — the caller doesn’t touch InventoryService, PaymentService, or ShippingService directly.\n\nNote vs Adapter/Decorator: facade doesn’t change or add behavior to one object — it SIMPLIFIES access to a whole group of them. (One method in, many calls out.)',
    '// Complex subsystem — several classes\nclass InventoryService { boolean reserve(String item) { return true; } }\nclass PaymentService   { void charge(double amt) { } }\nclass ShippingService  { void ship(String item) { } }\n\n// Facade — one simple method hides the orchestration\nclass OrderFacade {\n    private final InventoryService inventory = new InventoryService();\n    private final PaymentService payment    = new PaymentService();\n    private final ShippingService shipping  = new ShippingService();\n\n    public void placeOrder(String item, double price) {\n        if (!inventory.reserve(item))\n            throw new IllegalStateException("Out of stock");\n        payment.charge(price);   // coordinate the steps in order\n        shipping.ship(item);\n    }\n}\n\n// Caller does ONE thing, knows none of the internals:\n// new OrderFacade().placeOrder("Book", 20.0);'),

  // ---- Binary Search deck (interview flashcards) ----
  RC('rc-bs-core', 'Binary Search',
    'Binary search — the core idea, and the one precondition?',
    'Repeatedly halve the search space by asking a yes/no question at the middle, then discard the half that cannot contain the answer. Precondition: the space must be MONOTONIC — a sorted array, or a predicate that flips exactly once from false to true. Halving each step gives O(log n).'),
  RC('rc-bs-templates', 'Binary Search',
    'What are the two binary-search templates, and when do you use each?',
    'TEMPLATE A — exact match: inclusive bounds, loop while left <= right, return early when arr[mid] == target. TEMPLATE B — boundary search (left < right): for lower bound, upper bound, first/last occurrence, insertion point. You NEVER return early — keep shrinking until left == right, and that meeting point IS the answer. Prefer Template B; it generalizes to almost everything.',
    '// Template B — boundary search\nwhile (left < right) {\n    int mid = left + (right - left) / 2;\n    if (condition(mid)) right = mid;   // mid is a candidate — keep it\n    else                left = mid + 1;\n}\nreturn left;   // pointers meet at the answer'),
  RC('rc-bs-lowerbound', 'Binary Search',
    'Lower bound — what does it find, and how do the pointers move?',
    'The FIRST index whose value is >= target (the left-most insertion point). Condition: arr[mid] >= target means mid could be the answer, so KEEP it — right = mid. Otherwise left = mid + 1. Invariant: everything left of left is < target, everything from right onward is >= target. Meeting point = first index >= target (or n if none).'),
  RC('rc-bs-upperbound', 'Binary Search',
    'Upper bound — what does it find, and how is it different from lower bound?',
    'The FIRST index whose value is STRICTLY greater than target. The ONLY difference from lower bound is the comparison: arr[mid] > target (strict) instead of >=. Same pointer moves — right = mid when true, else left = mid + 1. Meeting point = first element > target (or n).'),
  RC('rc-bs-first', 'Binary Search',
    'How do you find the FIRST occurrence of a target?',
    'It IS the lower bound: the first index with arr[mid] >= target. Then verify arr[idx] == target, else the target is absent. Intuition: on equality you still move LEFT (right = mid), because an earlier equal element might exist — so you do not stop at the first equal value you happen to land on.'),
  RC('rc-bs-last', 'Binary Search',
    'How do you find the LAST occurrence of a target?',
    'It is (upper bound) − 1: find the first index strictly greater than target, then step back one. Verify arr[idx] == target. Intuition: on equality you move RIGHT to hunt for a later equal element, so upper bound lands just past the whole block of targets — one step back is the last one.'),
  RC('rc-bs-answer', 'Binary Search',
    'Binary search on the answer space — when does it apply, and how?',
    'Use it when you are not searching an array but a conceptual range of possible ANSWERS, and feasibility is MONOTONIC (once feasible it stays feasible in one direction). Binary search the smallest (or largest) value x for which a boolean check(x) holds. Example — Koko: search speed in [1, max(pile)]; check(k) = total hours = sum of ceil(pile / k) <= h; if feasible try slower, else faster; return the smallest feasible speed.'),
  RC('rc-bs-mistakes', 'Binary Search',
    'The common binary-search mistakes and pitfalls?',
    '• Overflow: use mid = left + (right − left) / 2, never (left + right) / 2. • Mixing templates: while left <= right WITH right = mid loops forever; while left < right WITH right = mid − 1 skips the answer. • Wrong comparison: >= gives lower bound, > gives upper bound — swapping them is a silent bug. • Forgetting to verify the found index actually equals the target. • Off-by-one bounds: right = n (half-open) vs n − 1 (inclusive). • Using it when the condition is NOT monotonic — then binary search is simply invalid.'),
  RC('rc-bs-checklist', 'Binary Search',
    'A mental checklist for attacking any binary-search problem?',
    '1) Is the space monotonic — sorted, or a false-to-true predicate? 2) What am I truly searching for: an exact value, a boundary, or a feasible answer? 3) Define the condition precisely. 4) Use the boundary template (left < right; true → right = mid; false → left = mid + 1). 5) Set bounds: array → right = n; answer range → right = maxAnswer + 1. 6) Decide the return: left, or left − 1 for last / maximum. 7) Dry-run the size-1, size-2, and not-found cases.'),
  // ---- Monotonic Stack & Queue deck ----
  RC('rc-mono-core', 'Monotonic Stack & Queue',
    'Monotonic stack/queue — what is it, and how is the order maintained (and why O(n))?',
    'A stack or queue whose elements are always kept sorted — increasing or decreasing. Before pushing a new element you POP everything that would break the order, then push. That pop-before-push is the whole trick. Each element is pushed and popped at most once, so total work is O(n) amortized despite the nested loop. Reach for it when the task is "for each element, find the next element matching some criteria" (next greater / next smaller), or when you need the max/min of a moving window.',
    'for (int num : nums) {\n    while (!stack.isEmpty() && stack.peek() > num)  // pop violators FIRST\n        stack.pop();\n    // ... problem logic goes here (record answer, etc.) ...\n    stack.push(num);\n}\n// increasing stack: pop while top > num.   decreasing: pop while top < num.'),
  RC('rc-mono-tree', 'Monotonic Stack & Queue',
    'Decision tree — stack vs deque, increasing vs decreasing?',
    'STEP 1 — Is it a SLIDING WINDOW? Yes → use a DEQUE (you must also evict elements that fall out of the window, from the front). No → use a STACK. STEP 2 — What are you tracking? Deque: MAX → decreasing deque, MIN → increasing deque. Stack: Next Greater Element → decreasing stack, Next Smaller Element → increasing stack.',
    'Sliding window?\n├─ YES → DEQUE   (also evict from the FRONT by window position)\n│    ├─ track MAX → monotonic DECREASING   (front = max)\n│    └─ track MIN → monotonic INCREASING   (front = min)\n└─ NO  → STACK   (top = next UNRESOLVED element)\n     ├─ Next Greater (NGE) → monotonic DECREASING\n     └─ Next Smaller (NSE) → monotonic INCREASING'),
  RC('rc-mono-order', 'Monotonic Stack & Queue',
    'Which order — increasing or decreasing — for what I want?',
    'DECREASING (largest at top/front): finds the NEXT GREATER element, and holds the window MAXIMUM at the front. INCREASING (smallest at top/front): finds the NEXT SMALLER element, and holds the window MINIMUM at the front. Intuition: a decreasing structure keeps big values waiting so the first later value bigger than them resolves them — that resolving value is their next-greater.',
    null),
  RC('rc-mono-leave', 'Monotonic Stack & Queue',
    'How do elements LEAVE — stack vs deque?',
    'STACK: an element stays until it finds its NGE/NSE — it is popped by the first later element that resolves it (the current element). So the top is always the next UNRESOLVED element. DEQUE: two exits — from the BACK you pop order-violators on insertion (same as a stack), and from the FRONT you drop elements that have fallen OUT of the window, by index/position. That front-eviction by window position is exactly why a sliding window needs a deque, not a stack.',
    null),
  RC('rc-mono-store', 'Monotonic Stack & Queue',
    'Do I store indices or values in the stack/deque?',
    'Store INDICES when the answer needs positions, distances, or widths — Daily Temperatures wants days-between = i − j; Largest Rectangle wants widths; a sliding window needs to know when an index falls out of range. Store VALUES only when the value itself is all you need. Default to indices: given an index you can always read nums[index], but not the reverse.',
    null),
  RC('rc-mono-problems', 'Monotonic Stack & Queue',
    'The canonical monotonic problems, and which structure each uses?',
    '• Daily Temperatures / Next Greater Element → monotonic DECREASING stack, store INDEX (you want the distance). • Sliding Window Maximum → monotonic DECREASING deque, store INDEX (front = max; pop the front once its index leaves the window). • Longest Subarray with abs(max − min) ≤ limit → sliding window + TWO deques (one INCREASING for the min, one DECREASING for the max). All run in O(n) amortized.',
    null),
  RC('rc-mono-tmpl-generic', 'Monotonic Stack & Queue',
    'Standard template — sliding window + monotonic deque (the general left/right form)?',
    'The deque stores INDICES; keep it monotonic DECREASING so the front is the window MAX (use INCREASING for the min). Three steps for each right: (1) pop from the BACK while it violates the order, then addLast(right); (2) shrink from the LEFT while the window is invalid, removing the front only if its index equals left as it leaves; (3) once the window is valid, read the front as the answer. This general form also handles VARIABLE windows — just change the "invalid" test: width > k for a fixed size, or max − min > limit for the abs-diff problem.',
    '// Sliding window + monotonic deque — deque stores INDICES\nDeque<Integer> dq = new ArrayDeque<>();   // decreasing: front = window max\nint left = 0;\nfor (int right = 0; right < n; right++) {\n    // 1) keep it monotonic: drop smaller values from the BACK\n    while (!dq.isEmpty() && nums[dq.getLast()] < nums[right]) dq.removeLast();\n    dq.addLast(right);\n    // 2) shrink from the LEFT while the window is invalid\n    while (windowInvalid()) {              // e.g. right - left + 1 > k\n        if (dq.getFirst() == left) dq.removeFirst();\n        left++;\n    }\n    // 3) window valid -> front is the answer\n    if (windowValid()) record(nums[dq.getFirst()]);\n}'),
  RC('rc-mono-tmpl-fixed', 'Monotonic Stack & Queue',
    'Fixed window size k — the cleaner monotonic-deque variant (no left pointer)?',
    'When the window is always exactly k, you do not need a left pointer or a width check. Evict the FRONT when its index is too old (dq.getFirst() <= right - k), and start recording once the first full window exists (right >= k - 1); the answer index is right - k + 1. Same back-eviction keeps it monotonic. Fewer moving parts than the general form — reach for this whenever k is fixed.',
    '// Fixed window of size k — no left pointer\nDeque<Integer> dq = new ArrayDeque<>();   // decreasing, stores indices\nint[] ans = new int[n - k + 1];\nfor (int right = 0; right < n; right++) {\n    // 1) keep it monotonic decreasing\n    while (!dq.isEmpty() && nums[dq.getLast()] < nums[right]) dq.removeLast();\n    dq.addLast(right);\n    // 2) evict the FRONT if it fell out of the window\n    if (dq.getFirst() <= right - k) dq.removeFirst();\n    // 3) record once the first full window exists\n    if (right >= k - 1) ans[right - k + 1] = nums[dq.getFirst()];\n}\nreturn ans;'),
  // ---- Heaps & Priority Queue deck ----
  RC('rc-heap-when', 'Heaps & Priority Queue',
    'When should I reach for a heap / priority queue?',
    'Trigger words: K largest, K smallest, Top K, K closest, K most frequent, Kth largest/smallest, best K. Also whenever you must REPEATEDLY pull the current max or min while the data keeps changing (simulations). Two families: (A) Top-K — keep a size-k heap of the best-so-far; (B) repeatedly-take-the-extreme — a full heap you pop the max/min from, often pushing a modified value back. A heap hands you the extreme in O(log n) even as elements are added and removed.'),
  RC('rc-heap-which', 'Heaps & Priority Queue',
    'Which heap do I use for which goal? (the key decision)',
    'Rule: the heap holds your candidates and the ROOT must be the WORST one, so each pop discards the worst. That means you use the OPPOSITE polarity of what you ultimately want. K Largest → MIN heap (root = smallest of your k = first to drop). K Smallest → MAX heap. K Closest → MAX heap (root = farthest → drop). Top K Frequent → MIN heap by frequency. Kth Largest → MIN heap of size k, answer = peek. For repeatedly-take-the-extreme you instead want the extreme itself at the root: MAX heap to keep pulling the largest, MIN heap to keep pulling the smallest.',
    'GOAL                       HEAP        root is...\nK largest                  MIN heap    smallest of the k (drop)\nK smallest                 MAX heap    largest of the k (drop)\nK closest                  MAX heap    farthest (drop)\nTop K frequent             MIN heap    least frequent (drop)\nKth largest (peek)         MIN heap    the answer\nrepeatedly take MAX        MAX heap    the max\nrepeatedly take MIN        MIN heap    the min\nmedian                     TWO heaps   the two middles'),
  RC('rc-heap-topk', 'Heaps & Priority Queue',
    'The Top-K heap template, and why it is O(n log k)?',
    'Keep a heap bounded at size k: offer each element, and the moment size exceeds k, poll the root (the worst). The k best survive. Because the heap never exceeds k, every operation is O(log k); over n elements that is O(n log k) — better than the O(n log n) sort. GOLDEN RULE: do not store everything, store only the k best seen so far.',
    'PriorityQueue<T> heap = new PriorityQueue<>(comparator);\nfor (T x : input) {\n    heap.offer(x);\n    if (heap.size() > k) heap.poll();   // drop the worst\n}\n// answer: heap.peek() (Kth)  OR  drain the heap (Top K)'),
  RC('rc-heap-comparator', 'Heaps & Priority Queue',
    'PriorityQueue comparator cheat sheet (Java)?',
    'A bare PriorityQueue is a MIN heap. The comparator returns negative when a should sit closer to the root. Always compare by the CRITERION, not the raw value. For large ints prefer Integer.compare(x, y) over x - y to avoid overflow.',
    'min heap:          (a, b) -> a - b\nmax heap:          (a, b) -> b - a\nmin by frequency:  (a, b) -> map.get(a) - map.get(b)\nmax by frequency:  (a, b) -> map.get(b) - map.get(a)\nmin by distance:   (a, b) -> Math.abs(a - x) - Math.abs(b - x)\nmax by distance:   (a, b) -> Math.abs(b - x) - Math.abs(a - x)'),
  RC('rc-heap-kth-stream', 'Heaps & Priority Queue',
    'Kth Largest in a Stream (LC 703) / Kth Largest in an Array (LC 215) — the pattern?',
    'Keep a MIN heap of size k. On each add: offer(val); if size > k poll(); return peek(). The root is always the kth largest, because the heap holds exactly the k largest values and its smallest (the root) is the kth. O(log k) per add. The array version (LC 215) is the same size-k min heap returning peek — or O(n) average with quickselect.',
    'PriorityQueue<Integer> heap = new PriorityQueue<>();  // MIN heap\n// constructor: add all nums, keep size k\npublic int add(int val) {\n    heap.offer(val);\n    if (heap.size() > k) heap.poll();\n    return heap.peek();   // kth largest so far\n}'),
  RC('rc-heap-kclosest-points', 'Heaps & Priority Queue',
    'K Closest Points to Origin (LC 973) — heap choice and the two gotchas?',
    'You want the k SMALLEST distances, so use a MAX heap (root = farthest = the one to drop). Two gotchas: (1) store the point (int[]) and compare by SQUARED distance x*x + y*y — never take the square root; comparing squares gives the same order and stays integer. (2) bound the heap to k and poll when it exceeds k. O(n log k).',
    'PriorityQueue<int[]> heap = new PriorityQueue<>(\n    (p, q) -> (q[0]*q[0] + q[1]*q[1]) - (p[0]*p[0] + p[1]*p[1]));  // MAX by dist^2\nfor (int[] pt : points) {\n    heap.offer(pt);\n    if (heap.size() > k) heap.poll();   // drop the farthest\n}\n// drain the heap -> the k closest points'),
  RC('rc-heap-topk-frequent', 'Heaps & Priority Queue',
    'Top K Frequent Elements (LC 347) — the two-step pattern + keySet vs entrySet?',
    'Step 1: build a frequency HashMap. Step 2: push keys into a MIN heap ordered BY FREQUENCY and poll when size > k, so the k most frequent survive. keySet() gives just the keys (the comparator reads map.get(key)); entrySet() gives (key, value) pairs — use whichever your comparator needs. O(n log k).',
    'Map<Integer,Integer> f = new HashMap<>();\nfor (int n : nums) f.merge(n, 1, Integer::sum);\nPriorityQueue<Integer> heap =\n    new PriorityQueue<>((a, b) -> f.get(a) - f.get(b));   // MIN by frequency\nfor (int key : f.keySet()) {\n    heap.offer(key);\n    if (heap.size() > k) heap.poll();\n}'),
  RC('rc-heap-simulation', 'Heaps & Priority Queue',
    'Repeatedly-take-the-extreme (Last Stone Weight, Connect Sticks, Min Stone Sum, Halve Array Sum)?',
    'NOT a size-k heap — put ALL elements in, then loop: pop the extreme, transform it, push the result back. The heap keeps handing you the current max/min in O(log n) even as you add modified values. Last Stone Weight: MAX heap, pop two, push their difference. Minimum Cost to Connect Sticks: MIN heap, pop the two smallest, push their sum and add to cost (greedy — always merge the two cheapest). Min Stone Sum / Halve Array Sum: MAX heap, pop the max, push its reduced value, repeat k times (or until the target is met).',
    '// Minimum Cost to Connect Sticks — MIN heap, greedy\nPriorityQueue<Integer> pq = new PriorityQueue<>();\nfor (int s : sticks) pq.offer(s);\nint cost = 0;\nwhile (pq.size() > 1) {\n    int a = pq.poll(), b = pq.poll();   // two smallest\n    cost += a + b;\n    pq.offer(a + b);                    // push combined stick back\n}\nreturn cost;'),
  RC('rc-heap-median', 'Heaps & Priority Queue',
    'Find Median from Data Stream (LC 295) — the two-heap trick?',
    'Keep a MAX heap for the lower half and a MIN heap for the upper half, balanced so their sizes differ by at most 1. addNum: push to the max heap, move its top to the min heap, and if the min heap is now bigger, move its top back — this keeps every lower value <= every upper value and the max heap holding the extra element when the count is odd. findMedian: sizes equal → average the two tops; otherwise the top of the bigger (max) heap. add is O(log n), findMedian is O(1).',
    '// maxHeap = lower half, minHeap = upper half; tops meet at the middle\naddNum(x):\n  maxHeap.offer(x);\n  minHeap.offer(maxHeap.poll());\n  if (minHeap.size() > maxHeap.size())\n      maxHeap.offer(minHeap.poll());\nfindMedian():\n  equal sizes -> (maxHeap.peek() + minHeap.peek()) / 2.0\n  else        -> maxHeap.peek()'),
  RC('rc-heap-bugs', 'Heaps & Priority Queue',
    'Common heap bugs to avoid?',
    '• size >= k should be size > k (>= drops one element too many). • Do not write while (k > 0) expecting k to shrink — it never changes. • Do not iterate for (i = 0; i < heap.size(); i++) and poll inside; heap.size() shrinks as you poll — use while (!heap.isEmpty()) or a fixed for (i = 0; i < k; i++). • Compare by the CRITERION, not the raw value (map.get(a) - map.get(b), not a - b). • Prefer Integer.compare(a, b) over a - b for large values to avoid overflow.'),
  RC('rc-heap-checklist', 'Heaps & Priority Queue',
    'The 6-question mental checklist for any heap problem?',
    '1) Is it a Top-K (or repeatedly-take-the-extreme) problem? 2) What makes an element good — value, frequency, distance? 3) Who is the worst? 4) Should the ROOT be the smallest or the largest by that criterion? 5) Can I keep the heap fixed at size k? 6) When it grows past k, poll the root. Answer these six and almost every common heap problem falls out.'),
  RC('rc-heap-build', 'Heaps & Priority Queue',
    'How does bottom-up heap construction (heapify) work, and why is it O(n) — not O(n log n)?',
    'A binary heap is stored as an ARRAY: node i has children at 2i+1 and 2i+2, and parent at (i-1)/2 — no pointers needed. To BUILD a heap from an unordered array, do not insert elements one-by-one (that would be O(n log n)). Instead go BOTTOM-UP: start at the last non-leaf node (index n/2 - 1) and move toward the root, calling SINK on each — pushing it down until its subtree obeys the heap order. Leaves are skipped because a single node is already a valid heap. When you reach any node, both of its child subtrees are already valid heaps, so one sink repairs the whole subtree — a series of small sinks building up to the full heap. WHY O(n): the work to sink a node is proportional to its HEIGHT, not log n. Half the nodes are leaves (height 0, zero work), a quarter are height 1, an eighth height 2, and so on — summing height x count over all levels is a convergent series that totals O(n). So the build is LINEAR even though the single tallest sink is O(log n).',
    '// build a min-heap in place, bottom-up — O(n)\nvoid heapify(int[] a) {\n    int n = a.length;\n    for (int i = n / 2 - 1; i >= 0; i--)   // last non-leaf → down to root\n        sink(a, i, n);\n}\n// each sink costs O(height of that node); summed over all nodes = O(n),\n// because most nodes are near the bottom with tiny height.'),
  RC('rc-heap-ops', 'Heaps & Priority Queue',
    'Why are heap add and remove both O(log n)? (swim vs sink)',
    'A heap is a COMPLETE binary tree (filled level by level), so its height is floor(log2 n). Every insert or delete walks just ONE root-to-leaf path = that height, which is why both are O(log n). ADD uses SWIM (sift-up): append the new value at the end of the array (the next open leaf), then swim it up — compare with its parent and swap while it violates the order, stopping when it fits or reaches the root. REMOVE-the-root (poll) uses SINK (sift-down): the root is the min/max you return; move the LAST element into the root slot to keep the array contiguous, shrink the size, then sink it — swap with its smaller child (for a min-heap) until it fits or reaches a leaf. Each walks at most the tree height, so each is O(log n). peek() is O(1) — the answer is always at index 0.',
    '// SWIM — used by ADD (0-indexed min-heap)\nvoid swim(int[] a, int i) {\n    while (i > 0 && a[(i - 1) / 2] > a[i]) {   // parent bigger → move up\n        swap(a, i, (i - 1) / 2);\n        i = (i - 1) / 2;\n    }\n}\n\n// SINK — used by REMOVE (and by heapify)\nvoid sink(int[] a, int i, int n) {\n    while (2 * i + 1 < n) {\n        int c = 2 * i + 1;                        // left child\n        if (c + 1 < n && a[c + 1] < a[c]) c++;    // pick the smaller child\n        if (a[i] <= a[c]) break;                  // already fits → stop\n        swap(a, i, c);\n        i = c;\n    }\n}'),
  // ---- LLD Approach deck ----
  RC('rc-lld-nfr', 'LLD Approach',
    'LLD non-functional requirements — the mnemonic to remember all five?',
    '"Pizza Shops Rarely Close Mondays" — the five non-functional requirements to raise in any design discussion, right after the functional ones:\n\n• Pizza → PERFORMANCE — latency and throughput; is the hot path fast enough?\n• Shops → SCALABILITY — does it hold up as data, users, and load grow?\n• Rarely → RELIABILITY — fault tolerance, no data loss, fail gracefully.\n• Close → CONCURRENCY — many actors at once; races on shared resources, locking.\n• Mondays → MAINTAINABILITY — clean, extensible (OCP), easy to change and test.\n\nNaming these after the functional requirements — especially CONCURRENCY on a shared resource — is a strong seniority signal in an LLD round.'),
  RC('rc-lld-entity', 'LLD Approach',
    'LLD — what is an entity?',
    'An **entity** is a real-world object or concept that has:\n• **Identity** — it can be told apart from other entities.\n• **Lifecycle** — it can be created, changed, and destroyed.\n• **State** — the data it currently holds.\n\nIn code, an entity is modeled as a **class**. Finding the entities — usually the **nouns** in the requirements — is the first step of any LLD (e.g. ParkingLot, Ticket, Vehicle).'),
  RC('rc-lld-lifecycle', 'LLD Approach',
    'LLD — what is an entity lifecycle?',
    'The **lifecycle** is the set of stages an entity passes through over time:\n• **Created** — brought into existence (constructed).\n• **Updated** — its state changes as things happen.\n• **Destroyed** — removed when it is no longer needed.\n\nExample: a parking Ticket is **created** on entry, **updated** as time accrues, and **destroyed** (or archived) once payment is done.'),
  RC('rc-lld-state', 'LLD Approach',
    'LLD — what is the state of an object?',
    '**State** = the **current values stored in an object fields** — the data the object holds right now.\n\nExample — a Company class: its state is name, contactInfo, employees, and so on. Change any field and you have changed the object state.\n\n**Encapsulation** protects state: keep fields private and change them only through methods that keep the object valid (its invariants intact).'),
  // ---- Time Complexity & TLE deck ----
  RC('rc-tc-bigo', 'Time Complexity & TLE',
    'Big-O — how do I simplify a complexity expression?',
    'Three steps:\n• Keep the **fastest-growing** term.\n• Drop **lower-order** terms.\n• Drop **constant** coefficients.\n\nExamples:\n• 2N + 100 → **O(N)**\n• 5N² + 3N + 500 → **O(N²)**\n• N² + N + 2^N → **O(2^N)**\n\nBig-O measures growth for **large** input, not exact operation counts.'),
  RC('rc-tc-ladder', 'Time Complexity & TLE',
    'The growth-order ladder — best to worst?',
    'From fastest to slowest as N grows. Anything at or below **O(N log N)** is usually safe for large N; **O(N²)** and worse only for small N.',
    'O(1)        constant\nO(log N)    halving each step (binary search)\nO(N)        single pass\nO(N log N)  sort / N × log work\nO(N²)       nested over N\nO(N³)       triple nested\nO(2^N)      subsets / brute force\nO(N!)       permutations\nO(N^N)      worst'),
  RC('rc-tc-loops', 'Time Complexity & TLE',
    'How do I read loops into a Big-O?',
    '• **Sequential** loops → ADD: loop N then loop M = **O(N + M)** (keep both if independent).\n• **Nested** complete loops → MULTIPLY: N × N = **O(N²)**, triple = **O(N³)**.\n• **Constant-bound** loop (1..100) → **O(1)**, whatever N is.\n• **Triangular** inner loop (j from 0..i): 1+2+…+N = N(N+1)/2 → **O(N²)**.\n• **Constant outer × N inner** (i up to 100, j up to N) → **O(N)** — the outer is constant.\n\nLesson: nested is NOT automatically O(N²) — count how many times the inner loop really runs.'),
  RC('rc-tc-log', 'Time Complexity & TLE',
    'When is a loop logarithmic — and what makes O(N log N)?',
    'A loop is **O(log N)** when its variable is **multiplied or divided by a constant** each step:\n• i = i * 2 until i ≥ N → log₂N steps.\n• i = N; i = i / 2 down to 1 → log₂N steps.\n\n**O(N log N)** = an N-loop with a log-loop inside (for each of N, double j up to N). Sorting is the classic O(N log N).\n\nThe log base is dropped in Big-O — it is only a constant factor.'),
  RC('rc-tc-value', 'Time Complexity & TLE',
    'Does the value a program computes affect its time complexity?',
    'No — complexity counts **operations**, not the size of the result.\n\n• result *= 2, N times → value is 2^N, but the loop runs N times → **O(N)**.\n• result *= N inside N×N loops → a huge number, but **O(N²)** operations.\n\nJudge complexity by **how many steps run**, never by how big the number gets.'),
  RC('rc-tc-constraints', 'Time Complexity & TLE',
    'Constraints → which complexity is fast enough? (contest gold)',
    'A short time limit allows roughly **10⁷–10⁸ operations**. Always plug in the **maximum** N, then pick the complexity that fits.',
    'max N          feasible complexity\n1e9            O(log N), O(1)\n1e7 – 1e8      O(N)\n1e5 – 1e6      O(N log N)   (N² is too slow)\n~1e4           O(N²) ok\n~500           O(N³) ok\n~20 – 25       O(2^N) ok\n~10 – 12       O(N!) ok\n\nN ≤ 1e5:   N³ = 1e15 ✗    N² = 1e10 ✗    N log N ≈ 1e6 ✓'),
  RC('rc-tc-tle', 'Time Complexity & TLE',
    'TLE — how do I estimate if my idea is fast enough BEFORE coding?',
    '**TLE = Time Limit Exceeded.** The judge caps run time (about 1 second).\n\nMental model:\n• A machine does about **10⁹** simple instructions per second.\n• One loop iteration ≈ 10–100 instructions.\n• So about **10⁷–10⁸ iterations** is the safe budget for ~1s.\n\nCheck: ops ≈ your Big-O evaluated at N = **maxN**. If that is above ~10⁸, it will **TLE** — change the idea before writing code.'),
  RC('rc-tc-workflow', 'Time Complexity & TLE',
    'The contest workflow that avoids the TLE-resubmit trap?',
    'Do not code-first and submit-hope. Estimate feasibility from the idea.\n\n• Read the **question**.\n• Read the **constraints** — they tell you the target complexity.\n• Form the **idea** / logic.\n• Check **correctness**.\n• Rough **pseudocode** (just the loop structure).\n• Compute the **complexity**.\n• Check **feasibility** against the constraints.\n• Only then **code**.\n\nConstraints → Idea → Complexity → Feasibility → Code.'),
  RC('rc-tc-debug', 'Time Complexity & TLE',
    'Loop debugging — the infinite-loop and array-index red flags?',
    'Infinite-loop causes:\n• Variable **never updated** (missing i++).\n• Update in the **wrong direction** (i-- needed, but i++ used).\n• Condition uses **!=** and the target is skipped (i += 2 never equals 10).\n• Variable starts at **0 and is multiplied** (0 * 2 = 0 forever).\n\nArray bounds:\n• Valid indices are **0 … length − 1**.\n• Forward: i < arr.length (not <=).\n• Backward: start at arr.length − 1 (not arr.length).'),
  // ---- Two Pointers & Arrays deck ----
  RC('rc-tp-when', 'Two Pointers & Arrays',
    'Two pointers — what is it, and what are the three setups?',
    'Two pointers = two index variables walking one or more sequences. It is an **idea, not one algorithm** — the starts and the moves depend on the problem.\n\nThree setups:\n• **Opposite ends** — left = 0, right = n − 1, move inward (palindrome, sorted two-sum, reverse, sorted squares).\n• **Two inputs** — i on array A, j on array B, advance one at a time (merge, subsequence).\n• **Same direction (fast / slow)** — both start left, move at different speeds (sliding window, cycle detection).\n\nInterview habit: do not just say two pointers were used — say WHY each pointer moves. The ordering or an invariant proves every discarded option is impossible or already handled.'),
  RC('rc-tp-opposite', 'Two Pointers & Arrays',
    'Pattern 1 — opposite ends: template and why it is O(N)?',
    'Start left = 0, right = n − 1; loop while left < right; each step move left, right, or both inward.\n\n**Why O(N):** the pointers begin n apart and every step moves at least one inward, so at most n total moves. With O(1) work per step → **O(N) time, O(1) space**.\n\nUsed by: palindrome, sorted two-sum, reverse-in-place, sorted squares.',
    'int left = 0, right = n - 1;\nwhile (left < right) {\n    // ... compare / act ...\n    left++;      // or right--, or both\n}'),
  RC('rc-tp-twoinputs', 'Two Pointers & Arrays',
    'Pattern 2 — two inputs: template and complexity?',
    'One pointer per input: i on A, j on B. Loop while **i < N AND j < M** — the AND matters: the instant either input is exhausted, comparison must stop, or you index out of bounds. Then handle leftovers in separate loops.\n\n**Why O(N + M):** i advances at most N times and j at most M → N + M total moves. Time **O(N + M)**.\n\nUsed by: merge two sorted arrays, subsequence check.',
    'int i = 0, j = 0;\nwhile (i < N && j < M) {        // AND, not OR\n    // compare A[i] vs B[j], advance one (or both)\n}\nwhile (i < N) { /* leftovers of A */ i++; }\nwhile (j < M) { /* leftovers of B */ j++; }'),
  RC('rc-tp-palindrome', 'Two Pointers & Arrays',
    'Palindrome check with two pointers?',
    'left = 0, right = n − 1. While left < right: if the two chars differ → return false; else move both inward. Reach the middle with no mismatch → true.\n\n• **Invariant:** everything outside [left, right] has already matched.\n• Java: this only READS, so charAt is enough — no char array needed.\n• **O(N) time, O(1) space.**',
    'while (left < right) {\n    if (s.charAt(left) != s.charAt(right)) return false;\n    left++; right--;\n}\nreturn true;'),
  RC('rc-tp-twosum', 'Two Pointers & Arrays',
    'Sorted Two Sum — which pointer moves, and why is it safe?',
    'Sorted array; left at the smallest, right at the largest. sum = a[left] + a[right]:\n• sum == target → found.\n• sum **> target** → right-- (the only way to shrink the sum).\n• sum **< target** → left++ (the only way to grow it).\n\n**Why safe:** sorted order makes the sum monotonic — if sum > target, no left move could reduce it, so right-- discards only impossible pairs.\n\n**O(N) time, O(1) space.** If the array is UNSORTED this rule breaks — use a **HashSet / HashMap** instead (O(N) time, O(N) space).'),
  RC('rc-tp-merge', 'Two Pointers & Arrays',
    'Merge two sorted arrays — condition, leftovers, and the space nuance?',
    'i, j start at 0. While **i < N AND j < M**: append the smaller of A[i], B[j] and advance only that pointer. Then append the leftovers of whichever array still has elements.\n\n• Use **AND** (not OR), or you index past an exhausted array.\n• Only one leftover loop actually runs.\n• Say it precisely: **O(N + M) time; O(N + M) output space, but O(1) auxiliary** if the returned array is not counted.',
    'while (i < N && j < M)\n    if (a[i] < b[j]) res.add(a[i++]);\n    else             res.add(b[j++]);\nwhile (i < N) res.add(a[i++]);   // leftovers\nwhile (j < M) res.add(b[j++]);'),
  RC('rc-tp-subseq', 'Two Pointers & Arrays',
    'Is S a subsequence of T — the pointer rules and the traps?',
    'i over S, j over T. Scan every char of T:\n• **j always advances** — every T char is examined once.\n• **i advances only on a match** — it marks the next needed char of S.\n• Answer = **i == S.length()** — every char of S was matched in order.\n\nTraps:\n• Do NOT return false on a mismatch — T chars may be skipped.\n• Do NOT return true on the first match — only when i reaches S.length().\n• Empty S → true (i already equals 0 = its length).\n\n**O(T) time, O(1) space.**',
    'int i = 0, j = 0;\nwhile (i < S.length() && j < T.length()) {\n    if (S.charAt(i) == T.charAt(j)) i++;\n    j++;\n}\nreturn i == S.length();'),
  RC('rc-tp-reverse', 'Two Pointers & Arrays',
    'Reverse a char array in place — approach and the classic bug?',
    'left = 0, right = n − 1. While left < right: swap s[left] and s[right] through a temp, then move both inward. It modifies the array directly (void, no return).\n\n• **Invariant:** everything outside [left, right] is already in its final reversed position.\n• **Classic bug:** initializing the pointers with VALUES (s[0], s[n-1]) instead of INDICES (0, n-1) — that reads characters, not positions, and breaks access.\n• **O(N) time, O(1) space.**',
    'int left = 0, right = n - 1;\nwhile (left < right) {\n    char tmp = s[left];\n    s[left]  = s[right];\n    s[right] = tmp;\n    left++; right--;\n}'),
  RC('rc-tp-squares', 'Two Pointers & Arrays',
    'Squares of a sorted array — the key idea and the two gotchas?',
    'The input is sorted, but negatives square large — so the **largest square is at one of the two ends**. Run two pointers from both ends and **fill the answer from the back** (largest square into the last empty slot).\n\nEach step: compare leftSq and rightSq, place the bigger at answer[k], move that pointer, then k--.\n\nGotchas:\n• Loop **while left <= right** (not <), or you skip the single middle element.\n• Large values? Square in **long** to avoid int overflow.\n\n**O(N) time, O(N) output, O(1) auxiliary.**',
    'int l = 0, r = n - 1, k = n - 1;\nint[] ans = new int[n];\nwhile (l <= r) {                    // <= keeps the middle element\n    long ls = (long) a[l] * a[l];\n    long rs = (long) a[r] * a[r];\n    if (rs > ls) { ans[k] = (int) rs; r--; }\n    else         { ans[k] = (int) ls; l++; }\n    k--;\n}\nreturn ans;'),
  RC('rc-arr-complexity', 'Two Pointers & Arrays',
    'Array & string operation complexities?',
    'Rules of thumb: a **known index** is O(1); **shifting or copying** many elements is O(N); **searching** without a helper structure is O(N).',
    'access by index            O(1)\nupdate by index            O(1)\nappend to dynamic array    amortized O(1)\nremove from the end        O(1)\ninsert / delete in middle  O(N)   (shift the rest)\nsearch (unsorted)          O(N)\nmodify immutable string    O(N)   (rebuild it)'),
  RC('rc-arr-mutable', 'Two Pointers & Arrays',
    'Mutable vs immutable — the Java string rule?',
    'Mutable = changeable after creation (arrays, char[], ArrayList). Immutable = cannot change (Java String, Python str) — modifying builds a NEW object, costing O(N).\n\nJava rule of thumb:\n• **Read chars only** → String + charAt (O(1) read).\n• **Modify in place** → convert to a **char[]**.\n• **Build / append repeatedly** → **StringBuilder** (avoids O(N) per append).'),
  RC('rc-tp-mistakes', 'Two Pointers & Arrays',
    'Common two-pointer mistakes?',
    '• Assuming pointers always start at opposite ends — they may start together, in different inputs, or move the same direction at different speeds.\n• Using the sorted-two-sum move rule on an **unsorted** array — it needs a hashmap instead.\n• Subsequence: returning false on a mismatch, or true on the first match (true only when i == S.length()).\n• Merge: using **OR** in the main loop (indexes past an exhausted array), or forgetting the leftovers.\n• Reverse: initializing the pointers with values instead of indices.\n• Sorted squares: using left < right (skips the middle), or moving both pointers in one step.'),
  RC('rc-tp-template', 'Two Pointers & Arrays',
    'The interview explanation template for any two-pointer problem?',
    'For every two-pointer solution, be ready to state:\n• Where each pointer **starts** and what it **represents**.\n• The **loop condition**, and what makes each pointer **move**.\n• Why that move is **safe** — the invariant.\n• When it **stops**, the **edge cases**, and **time + space**.\n\nThe invariant is the whole game:\n• Palindrome / reverse — everything outside [left, right] is already done.\n• Sorted two-sum — every discarded pair cannot reach the target.\n• Merge — the result holds the smallest elements so far, in order.\n• Subsequence — i = how many chars of S have matched in order.\n• Sorted squares — everything after k already holds the largest squares, sorted.\n\nThe line to say: I move this pointer because the **ordering or invariant proves every discarded option is impossible or already processed.**'),
  // ---- Sliding Window deck ----
  RC('rc-sw-decide', 'Sliding Window',
    'Sliding window — what is it, and the first decision: fixed or dynamic?',
    'Sliding window works on **contiguous** sections — a **subarray** (array) or **substring** (string). The window is [left, right]; its length is **right − left + 1**.\n\nFirst decision — fixed or dynamic?\n• The problem names an **exact size k** (subarray of length k, k consecutive elements) → **FIXED window**.\n• The problem gives a **validity condition** (sum <= k, at most one zero, at most k distinct, product < k) → **DYNAMIC window**.\n\nMemory rule: exact length given → fixed; a condition controls the length → dynamic.'),
  RC('rc-sw-fixed', 'Sliding Window',
    'Fixed-size sliding window — template and when to use it?',
    'Use when every window must have **exactly k** elements. Wording: max sum or max average of k consecutive, substring of size k.\n\nHow it moves: build the first window of k, then each step **add the entering element (index right) and remove the leaving one (index right − k)**, and update the answer.\n\nWhy right − k: when index right enters, the element k positions behind it leaves (right = 4, k = 3 → index 1 leaves).\n\n**O(N) time, O(1) space.**',
    'int cur = 0;\nfor (int i = 0; i < k; i++) cur += nums[i];   // first window\nint ans = cur;\nfor (int right = k; right < n; right++) {\n    cur += nums[right];         // element enters\n    cur -= nums[right - k];     // element leaves\n    ans = Math.max(ans, cur);\n}\nreturn ans;'),
  RC('rc-sw-dynamic', 'Sliding Window',
    'Dynamic sliding window — the core loop, and metric vs restriction?',
    'Use when the window length is not fixed; a **validity condition** decides it.\n\nCore loop: **expand right** (add the new element), then **while the window is invalid, shrink from left**, then update the answer while valid.\n\nName two things for any problem:\n• **Metric** — the value you track for the window (sum, product, zeroCount, distinctCount).\n• **Restriction** — the rule that makes it valid (sum <= k, product < k, zeroCount <= 1, distinct <= k).\n\nExample: longest substring with at most one zero → metric = zeroCount, restriction = zeroCount <= 1.'),
  RC('rc-sw-longest', 'Sliding Window',
    'Dynamic window — LONGEST valid window template?',
    'For the longest subarray/substring: **shrink while INVALID**; once the loop exits the window is valid, so take the max length.\n\n• Add nums[right] to the metric.\n• while (invalid): remove nums[left], left++.\n• answer = max(answer, right − left + 1).\n\nExample below — longest subarray with sum <= k (non-negative numbers): metric = sum, shrink while sum > k.',
    'int left = 0, sum = 0, ans = 0;\nfor (int right = 0; right < n; right++) {\n    sum += nums[right];\n    while (sum > k) {           // shrink while INVALID\n        sum -= nums[left];\n        left++;\n    }\n    ans = Math.max(ans, right - left + 1);\n}\nreturn ans;'),
  RC('rc-sw-shortest', 'Sliding Window',
    'Dynamic window — SHORTEST valid window template (and the key difference)?',
    'For the shortest subarray/substring satisfying a condition: once the window is **VALID, record the min length and keep shrinking while it stays valid**.\n\n**The key difference:**\n• Longest → shrink while INVALID, then take max.\n• Shortest → once VALID, take min and keep shrinking while still valid.\n\nExample — shortest subarray with sum >= k (positive numbers). Return 0 (via a MAX_VALUE sentinel) if no window ever qualifies.',
    'int left = 0, sum = 0, ans = Integer.MAX_VALUE;\nfor (int right = 0; right < n; right++) {\n    sum += nums[right];\n    while (sum >= k) {                    // valid → try to shrink\n        ans = Math.min(ans, right - left + 1);\n        sum -= nums[left];\n        left++;\n    }\n}\nreturn ans == Integer.MAX_VALUE ? 0 : ans;'),
  RC('rc-sw-count', 'Sliding Window',
    'Dynamic window — COUNTING valid subarrays template (why right − left + 1)?',
    'When the question asks HOW MANY valid subarrays: keep the window valid, then **add right − left + 1** each step.\n\nWhy: for the current right, valid subarrays can start at left, left+1, …, right — that is **right − left + 1** subarrays ending at right. Summing over all right counts them all.\n\nExample — number of subarrays with product < k (positive integers): metric = product, shrink while product >= k.',
    'int left = 0, product = 1, ans = 0;\nfor (int right = 0; right < n; right++) {\n    product *= nums[right];\n    while (product >= k) {         // keep the window valid\n        product /= nums[left];\n        left++;\n    }\n    ans += right - left + 1;      // subarrays ending at right\n}\nreturn ans;'),
  RC('rc-sw-decision', 'Sliding Window',
    'Sliding window — which template does this question need?',
    'Match the phrasing to the template:',
    'question phrasing                          template\nmax sum / avg of k consecutive             FIXED size\nsubstring of size k                        FIXED size\nlongest subarray with sum <= k             DYNAMIC longest\nlongest substring, at most one zero        DYNAMIC longest\nlongest substring, <= k distinct chars     DYNAMIC longest + HashMap\nnumber of subarrays with product < k       DYNAMIC counting\nshortest subarray with sum >= k            DYNAMIC shortest'),
  RC('rc-sw-cheat', 'Sliding Window',
    'Sliding window — the answer-update cheat sheet?',
    'The update line tells you which template you are in:',
    'longest valid window    ans = Math.max(ans, right - left + 1)\nshortest valid window   ans = Math.min(ans, right - left + 1)   (while valid)\ncount valid subarrays   ans += right - left + 1\nfixed-window metric     ans = Math.max(ans, currentSum)\n\nfixed   : add one, remove one, size stays k\ndynamic : add right, shrink left when invalid, size changes'),
  // ---- Leadership Principles (STAR stories) deck ----
  RC('rc-lp-map', 'Leadership Principles',
    'My LP → story map (which story for which principle)?',
    'Aim for one strong story per principle; several stories each cover 2-3 LPs. Current coverage (pending = story to add):',
    'Are Right, A Lot      search retrieval hypothesis (no query data)\nBias for Action       time-zone hotfix / auto Weaviate sync\nCustomer Obsession    Insights page from CSAT / async EHR sync\nDeliver Results       Aura to LWC migration (50+ comps)\nDive Deep             Directus/Postgres migration incident\nOwnership             Directus incident to long-term fixes\nInvent and Simplify   incremental CMS builds\nLearn and Be Curious  learning hybrid retrieval\nThink Big             shared component library\nDisagree and Commit   search precision disagreement    (pending)\nHighest Standards     Jest/TDD testing framework       (pending)\nEarn Trust            critical feedback                (pending)\nHire and Develop      peer coaching                    (pending)\nFrugality             Terraform automation             (pending)'),
  RC('rc-lp-aral1', 'Leadership Principles',
    'Are Right, A Lot — chose a search retrieval approach with no query logs',
    '**S:** New search on InnovateUS (150+ agencies, ~5-6k DAU); no query logs, so I had to assume how users would search.\n**T:** Pick a retrieval approach before evidence, and keep it easy to change if wrong.\n**A:** Treated my instinct (nontechnical users type full questions) as a hypothesis; reviewed sample queries with my manager and content stakeholders; chose Weaviate **hybrid** search (BM25 for short queries, vector for long) with reranking, parallel collections, and caching; tested BM25-only vs hybrid on a labeled query set.\n**R:** Shipped in **2 weeks**, **+45% relevance** vs the BM25 baseline, **P95 < 2.5s**, avoided about a week of rework.\n**Learning:** Treat instincts as hypotheses — name what could disprove them and validate with people closer to the customer before committing.'),
  RC('rc-lp-aral2', 'Leadership Principles',
    'Are Right, A Lot — owned a wrong call: prerender-everything → incremental builds',
    '**S:** ~200 dynamic pages (Nuxt, Directus, Netlify); I first prerendered **every** page on **every** deploy.\n**T:** Keep pages fast, SEO-friendly, and always showing the latest CMS content.\n**A:** As content grew, builds became slow and fragile — one failed Directus call left a page missing. I recognized full regeneration did not scale; redesigned to **reuse unchanged pages** and regenerate only new/updated/missing ones, moved CMS fetch into server-safe functions, and added render validation.\n**R:** Cut needless regeneration across ~200 routes, made deploys predictable, and reduced incomplete pages — while keeping static speed and SEO.\n**Learning:** Architecture must account for **deployment reliability** and update frequency, not just runtime performance.'),
  RC('rc-lp-bfa1', 'Leadership Principles',
    'Bias for Action — production time-zone bug (reversible hotfix)',
    '**S:** After launch, workshop publish times were wrong — editors entered local time, Directus stored it without the zone, so 5pm Pacific showed as 5pm Eastern.\n**T:** Correct existing data fast and stop new wrong publishes.\n**A:** Used Directus activity logs (timestamp + editor) to write a **one-time correction script** (local to UTC); added an **event-driven flow** normalizing publish time to UTC on create/update; removed the workaround once Directus captured the zone properly.\n**R:** Existing times corrected, new updates auto-normalized, users saw correct go-live times — and it bought time for the proper long-term config.\n**Learning:** Acting fast in production does not mean a permanent hack — contain impact with a **reversible** fix, then replace it with the clean solution.'),
  RC('rc-lp-bfa2', 'Leadership Principles',
    'Bias for Action — auto-indexed search before it became a bottleneck',
    '**S:** Editors constantly created and updated workshops, but search reindexing was manual.\n**T:** Even though my task was just search, I saw manual reindexing would become an operational problem as more editors published.\n**A:** Proposed **event-driven indexing**: Directus webhooks trigger a Netlify Function that cleans, chunks, and syncs content into Weaviate on every create/update/delete; used **deterministic IDs** so repeats update the existing doc instead of duplicating.\n**R:** Editors no longer thought about indexing, results stayed synchronized automatically, and a manual step was removed before it grew into a maintenance issue.\n**Learning:** Think beyond the feature — spot the operational work that becomes a bottleneck as the system scales.'),
  RC('rc-lp-co1', 'Leadership Principles',
    'Customer Obsession — validated a need with a survey, built the Insights page',
    '**S:** LTI / Hartford insurance platform processed transactions well but gave agents no visibility into their own performance; there was no formal request.\n**T:** Validate whether this was a real customer need, not just my assumption.\n**A:** Ran a **2-week CSAT survey** embedded in the app; about **30%** gave low ratings, consistently asking for performance visibility (bind rate, pending quotes, sales over time); presented findings, which became a product epic; built a **Spring Boot** service exposing aggregated warehouse data via REST to power a new **Insights page** (credentials stayed server-side).\n**R:** Turned existing data into actionable insight with no extra data entry — agents could track performance and pipeline; the feature was backed by validated feedback, not assumptions.\n**Learning:** Anticipate needs, but validate them with **data from customers** before building.'),
  RC('rc-lp-co2', 'Leadership Principles',
    'Customer Obsession — pushed back on a slow requested solution (async EHR sync)',
    '**S:** Huron Salesforce Health Cloud; an ops lead wanted coordinators to **wait** for EHR confirmation on each patient-status update to avoid mismatched records.\n**T:** Keep the systems in sync without making users wait on a slow downstream system.\n**A:** Proposed saving in Salesforce first, then syncing to the EHR **asynchronously via Platform Events**; added sync-status tracking, duplicate protection, and failure handling so ops kept visibility.\n**R:** **~40% lower** user-facing latency with reliable EHR sync — coordinators kept working, and ops still saw failed or pending updates.\n**Learning:** Preserve the **real customer need** (reliable updates) rather than blindly following a requested solution that adds delay.'),
  RC('rc-lp-dr1', 'Leadership Principles',
    'Deliver Results — migrated 50+ Aura components to LWC on deadline',
    '**S:** Huron Salesforce Health Cloud on legacy Aura; leadership set an end-of-quarter goal to migrate critical components to LWC — in production, no disruption allowed.\n**T:** Migrate my **50+ components** on time, preserve functionality, and improve performance.\n**A:** Grouped components by risk, complexity, and dependency; built a **repeatable process** (convert markup + JS, reuse Apex, replace Aura events, add regression tests); migrated in **small batches**; found shared event/state dependencies, so I grouped dependent components and reordered; validated workflows after each batch.\n**R:** **50+ components** migrated on schedule, **+40% render performance**, **−30% browser memory**; the repeatable approach made the rest predictable.\n**Learning:** Organize large modernizations by **risk and dependency**, and adjust the plan when hidden dependencies appear.'),
  RC('rc-lp-dr2', 'Leadership Principles',
    'Deliver Results — realized coverage was the wrong metric (chased relevance)',
    '**S:** Tuning workshop search, I first maximized **coverage** (share of searches returning a result); loosening matching raised coverage but made exact-title and instructor searches return loosely related results.\n**T:** Fix the goal — relevance, not coverage — without delaying delivery.\n**A:** Split eval queries into categories (exact titles, instructor names, topics, broad questions, expected no-result); measured whether the **correct** result ranked near the top; tightened fuzzy and exact matching, kept semantic for broad questions, and allowed **no result** when confidence was low.\n**R:** **+45% relevance** on the labeled set vs the BM25 baseline, **P95 < 2.5s**; exact searches became reliable while broad questions still used semantic retrieval.\n**Learning:** A metric can drive the **wrong behavior** — fewer but relevant results beat always returning something.'),
  RC('rc-lp-dr3', 'Leadership Principles',
    'Deliver Results — missed a deadline (component library) and handled it openly',
    '**S:** Built a shared **Vue component library (30+ components)** for 5 apps; committed to v1 in a 2-week sprint while also building a separate full-stack app for a stakeholder.\n**T:** Balance the stakeholder delivery with the library commitment.\n**A:** Built a versioned package (design tokens, accessibility, Storybook, CI/CD). I underestimated meetings, reviews, and testing; first tried to recover the full scope alone, then **told my manager**, negotiated with the dependent team, shipped the **core components first** (buttons, inputs, dropdowns, modals, nav, form validation), and deferred the rest.\n**R:** Missed the 2-week commitment (full v1 about **6 weeks** late), but the dependent app got the critical components first and kept moving; the library grew to **30+ components**; the stakeholder app also shipped.\n**Learning:** Estimate on **total capacity**, and communicate risk early and negotiate scope instead of silently protecting every commitment.'),
  RC('rc-lp-dd', 'Leadership Principles',
    'Dive Deep — root-caused a production CMS outage (migration backfill)',
    '**S:** Self-hosted Directus + PostgreSQL served several apps; a new field triggered an ALTER TABLE plus a large **backfill UPDATE**. Fine in staging (small data), but production had years of data, so editors hit **502s** and APIs slowed.\n**T:** Restore production, find the true root cause, and prevent recurrence.\n**A:** Isolated it layer by layer (VM, Docker, Postgres, network all healthy); logs showed requests **waiting on the DB**; inspected running queries and found the backfill UPDATE holding contention on the workshops table. **Rolled back** the deploy, safely terminated the long transaction, verified consistency via audit logs, then reproduced it on prod-sized data.\n**R:** Restored **without data loss**; added maintenance windows, migration reviews, **batched SQL** backfills, and rollback docs; the org later moved to **Directus Enterprise** to cut ops overhead.\n**Learning:** Correctness is not operational safety — a migration fine in staging can break production at scale; fix the **deeper weakness**, not just restore service.'),
  RC('rc-lp-own', 'Leadership Principles',
    'Ownership — stepped into an outage I did not own and drove the long-term fix',
    '**S:** The same self-hosted Directus outage; I was **not** the infra owner, but it affected multiple teams.\n**T:** Restore production, and make sure it was not treated as just another outage.\n**A:** Investigated from infra down to Postgres, found the long-running migration contention, restored service, and reproduced it; then **documented findings and proposed** migration reviews, maintenance windows, batched updates, rollback docs, and backup validation; at the postmortem I surfaced the ops burden of self-hosting.\n**R:** Production restored, future deploys much safer, and my case contributed to leadership moving to **Directus Enterprise** — freeing engineers from infra work.\n**Learning:** Ownership does not end when production is back online — it improves the system so future engineers do not hit the same problem. (Same incident as the Dive Deep story — pick the framing the question calls for.)'),
]

// ---------------------------------------------------------------------------
export const seedResources = [
  { id: 'r-topics', category: 'Official', title: 'Amazon Software Development Topics', url: 'https://www.amazon.jobs/en/landing_pages/software-development-topics', note: 'The technical areas listed in your invite.' },
  { id: 'r-lp', category: 'Official', title: 'Amazon Leadership Principles', url: 'https://www.amazon.jobs/content/en/our-workplace/leadership-principles', note: 'The 16 LPs — memorize the themes, not the words.' },
  { id: 'r-prep', category: 'Official', title: 'Interviewing at Amazon', url: 'https://www.amazon.jobs/content/en/how-we-hire/interviewing-at-amazon', note: 'Official prep guide.' },
  { id: 'r-star', category: 'Official', title: 'STAR Method (Amazon)', url: 'https://www.amazon.jobs/content/en/how-we-hire/interview-loop', note: 'Situation, Task, Action, Result.' },
  { id: 'r-neetcode', category: 'Coding', title: 'NeetCode 150', url: 'https://neetcode.io/practice', note: 'The pattern-based set that backs your Coding tab.' },
  { id: 'r-blind75', category: 'Coding', title: 'Blind 75', url: 'https://www.teamblind.com/post/New-Year-Gift---Curated-List-of-Top-75-LeetCode-Questions-to-Save-Your-Time-OaM1orEU', note: 'Classic 75-problem list.' },
  { id: 'r-lc-amazon', category: 'Coding', title: 'LeetCode — Amazon tagged', url: 'https://leetcode.com/company/amazon/', note: 'Recently reported Amazon questions (Premium).' },
]

export const seedTodos = [
  { id: 't-confirm', text: 'Reply to Erika confirming receipt + availability', due: '2026-07-18', done: true, priority: 'high' },
  { id: 't-livecode', text: 'Save the LiveCode link once it arrives', due: '', done: false, priority: 'high' },
  // Phase 1 — the 7 dedicated DSA days
  { id: 't-dsa', text: 'DSA sprint: 7 focused days, trees & graphs first, high-frequency problems prioritized', due: '', done: false, priority: 'high' },
  // Phase 2 — the final ~4 days (everything else)
  { id: 't-stories', text: 'Build a bank of 8–10 STAR stories, each mapped to 2–3 Leadership Principles', due: '', done: false, priority: 'high' },
  { id: 't-resume', text: 'Prep dive-deep answers for every résumé project (metrics, tradeoffs, “why X not Y”)', due: '', done: false, priority: 'high' },
  { id: 't-lld', text: 'Practice core LLD/OOD: parking lot, vending machine, ATM, LRU/LFU', due: '', done: false, priority: 'medium' },
  { id: 't-mock', text: 'Do 1–2 full timed mock loops (coding + behavioral)', due: '', done: false, priority: 'medium' },
  { id: 't-questions', text: 'Prepare 3–4 thoughtful questions to ask interviewers', due: '', done: false, priority: 'medium' },
  { id: 't-logistics', text: 'Confirm travel/hotel after Save-the-Date email', due: '', done: false, priority: 'medium' },
]
